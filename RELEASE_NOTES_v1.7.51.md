# BrowserStudio v1.7.51

## 扩展分配逻辑修复

- 全局扩展改为完全被动执行：仅当用户在扩展管理中点击“分配”时，才检查并安装缺失的扩展。
- 保存全局配置、创建单个环境、批量创建环境和启动环境均不再触发全局扩展同步。
- 全局分配会记录已处理的环境编号；再次点击时跳过已完成环境，新建环境则在用户再次点击“分配”后单独检查。
- 已存在的同 ID、同名称或同来源扩展不会被覆盖或替换；现有扩展、钱包、Cookie、登录状态和环境数据保持不变。
- 扩展启动参数仅负责让已分配扩展正常可用，不执行后台扫描、下载、覆盖或实时同步。

## 环境删除与同步助手

- 删除环境改为完整删除：环境必须先停止，随后删除其专属浏览器数据、扩展/钱包存储、Cookie、缓存和快照，不再在 `data` 中留下无主目录。
- 删除时同步清理该环境的扩展分配引用；共享扩展程序包和其他环境的数据不受影响。
- 同步助手优先复用主客户端发布的有效窗口句柄，仅对缺失句柄的环境进行一次批量进程树扫描，减少 20+ 窗口刷新开销。
- 修复平铺、堆叠或横排后的窄小 Chrome 窗口被误判为“无窗口”的问题，打开同步助手或点击刷新后可立即全选已启动环境。

## 升级说明

- 支持由 v1.7.50 及更早版本直接在线升级到 v1.7.51。
- 升级不覆盖现有 `data`、浏览器环境、扩展、钱包、Cookie、代理池或激活数据。
