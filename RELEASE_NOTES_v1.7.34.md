# BrowserStudio v1.7.34

## 20 个以上环境的同步选择

- 修复同步助手已经读取窗口，但无法稳定全选或设置主控的问题。
- Chrome 将窗口从启动进程转交给同级进程后，同步助手本地确认的真实 PID 不再被旧共享快照反复覆盖。
- 某一次刷新短暂显示“无窗口”时，不再清空用户已经勾选的环境和主控；真正离开运行列表后才清理选择。
- 保留批量进程树的一次扫描逻辑，不恢复逐窗口的高成本轮询。

## 独立 HTTP、HTTPS、SOCKS5 代理

- 代理编辑器改为协议、地址、端口、用户名和密码的结构化字段，标准代理无需再手工修改整段 URL。
- 新增“验证并识别协议”，通过真实认证 HTTP 请求判断互联网是否可用，而不是把 ping 或 TCP 端口开放误认为代理可用。
- 供应商列表协议标注错误时，会并行尝试 HTTP、HTTPS 和 SOCKS5，并自动采用真正可用的协议。
- 标准代理启动期协议探测从最长串行等待改为有界并行探测，避免 20 个环境因错误协议等待数分钟。
- 编辑器明确提示：环境独立代理不要求开启 VPN TUN；必须同时使用 TUN 时，应将上游代理服务器 IP 加入路由排除，避免双重代理或回环。
- 日志不再写入完整代理 URL、用户名密码或上游认证配置。

## 升级兼容

- 本次更新不覆盖 data、Cookies、浏览器扩展、钱包数据、代理池、内核或激活状态。
- v1.7.18–v1.7.33 可直接在线升级到 v1.7.34，无需逐版本安装。
