# BrowserStudio v1.7.112

## 修复内容

- 修复从 `v1.7.84` 起已正常使用、但在近期版本升级后出现“环境内扩展消失”的兼容问题。启动时不再改写 Chromium 的 `Preferences`、`Local State`、固定标签或扩展登记，因此不会再触发 Chrome 的“这些设置已在外部更改，已重置扩展程序”保护。
- 已有环境会在每次用户启动环境时执行一次只读、瞬时恢复：若 Chrome 的扩展登记已被旧版重置，但 `Default/Extensions/<扩展 ID>/<版本>` 仍在，则只为该次启动加载该用户原有扩展。Chrome 商店扩展（通常没有 manifest 公钥）也能识别；有公钥但 ID 不一致的包会拒绝加载，避免形成第二个钱包实例。
- 用户在浏览器中自行安装的扩展始终优先。客户端“扩展管理”不再在启动时自动分配、注入、重写或巡检；已存在的用户扩展、钱包、Cookies、Local Extension Settings 和 IndexedDB 不会被替换。
- 删除旧的扩展主页/弹窗关闭器与持续弹窗约束轮询。浏览器或网页在用户操作时拉起的扩展页面保持正常可用；应用不再在后台关闭它们。
- 修正扩展分配重复记录：同一管理器扩展只在用户明确点击分配时记录一次；新建环境后再次点击分配才会处理新环境。
- 修复 Windows 修复升级脚本默认目标版本，版本化修复脚本默认指向 `v1.7.112`。

## 数据安全

- 本次不删除、不覆盖、不读取用户的助记词、私钥、钱包密码、Cookies、扩展存储或浏览器登录状态。
- 只读取 `Default/Extensions` 下的扩展程序文件夹与 manifest 来恢复旧 Chrome 登记丢失后的可用性；不会修改其中任何文件。

## 验收方式

1. 更新后打开一个升级前已自行安装 MetaMask、Rabby 或其他扩展的环境。
2. 打开 `chrome://extensions`，确认原扩展可见、启用，且钱包/登录状态保持原状。
3. 关闭并再次打开同一环境，确认不出现 Chrome “外部更改后重置扩展”的提示。
4. 在网页或扩展按钮中手动打开钱包，确认其页面可正常显示；客户端不应在后台关闭该页面。
