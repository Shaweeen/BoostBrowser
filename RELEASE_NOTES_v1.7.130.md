# BrowserStudio v1.7.130 发布说明

## 修复：TUN 接管模式下代理池测速 / IP 健康检测全部“超时/不可用”

### 问题现象
开启「TUN 接管」模式（或本机 Clash 开 TUN）后，导入的代理池点击测速 / IP 健康，
节点全部显示“超时/不可用”，无法通过代理测试。截图里连内置的「本地代理」行
（指向 127.0.0.1:7890）也超时。

### 根因（三层）
1. **v1.7.129 只给 auto 模式加了「直连失败 → 回退本地 VPN 网关」；tun 模式池测速 /
   IP 健康仍只走直连。** TUN 接管模式下，应用进程的直连会被本机 Clash TUN 截获、
   按 Clash 规则转发——节点 IP 从代理出口访问通常连不上代理服务器 → 全部超时。
   网关地址（127.0.0.1:7890/7897 等）是回环地址，不会被 TUN 截获，才是 TUN 模式
   下环境的真实路径。
2. **内置「本地代理」行硬编码 `http://127.0.0.1:7890`。** 大量用户（Clash Verge Rev
   等）的 Clash 实际监听在 **7897** 等端口，7890 没有服务 → 该行永远“超时/不可用”，
   且批量「测试全部 / 重测失败 / 检测IP健康」把它算进失败数，造成“代理全部不可用”
   的观感。
3. **内置行经网关测自己（自环/双跳）**：当「本地代理」指向的地址恰好就是网关本身时，
   local_gateway / tun 模式会把它经网关再拨号一遍，自环或双跳，延迟虚高甚至失败。

### 修复内容
- **tun 模式测速（speedtest.go）**：改为「本地网关优先 → 直连回退」；网关不可用时
  在错误信息里附上可操作诊断（提示检查 Clash / 填写本地网关地址），不再只显示笼统
  的“TCP 连接失败”。
- **tun 模式 IP 健康（iphealth.go）**：同样「网关优先 → 直连回退」，确保拿到的是
  代理池节点的真实出口 IP，而不是被 TUN 截获后变成的 Clash 节点出口。
- **内置「本地代理」动态指向实际网关**：启动/保存代理时探测本地 VPN 网关
  （7890/7897/7891/10808/10809 等），把内置行刷新为检测到的地址；未检测到网关时
  保持已有配置（不覆盖）。单个测速「本地代理」时也实时解析当前网关。
- **节点 == 网关时直连测试**：local_gateway / tun 模式下，若节点 host:port 与本地
  网关完全一致，直接直连测试本机服务，避免经网关测自己的自环/双跳。
- **批量测速/重测失败/检测IP健康排除内置行**：内置「直连 / 本地代理」不是用户代理，
  不再参与批量统计，可用/失败数字只反映真实节点。
- 新增 5 个回归测试：tun 直连被截断时经网关测速成功（模拟真实 TUN 场景）、tun 无
  网关回退直连、direct 模式绝不接触网关、tun IP 健康经网关拿到真实出口 IP、
  节点==网关判定与直连测试。

### 验证
`go build` / `go vet` / 全量 `go test ./...` 全绿、前端 `tsc --noEmit` 零错误、
30 个打包测试通过。

### 使用说明
- 模式保持「自动」或「TUN 接管」均可：池页测速 / IP 健康会自动经本机网关（Clash
  7890/7897 等，自动探测；也可在设置中填写本地网关地址）验证节点。
- 若本机 Clash 的混合端口不是常见端口，请在设置里填写「本地网关地址」，否则网关
  探测不到会回退直连（TUN 下仍可能失败）。
- 「本地代理」行现在跟随你实际的 Clash 端口显示，且不参与批量测速统计。
