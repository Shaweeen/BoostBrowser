# BrowserStudio v1.7.31

## 批量环境启动提速

- 批量启动由逐个等待改为按电脑逻辑核心自动使用 2–4 个启动工作槽，兼顾速度与资源稳定性。
- Chrome 进程创建后立即登记运行状态并释放全局管理锁，不同环境的调试接口就绪等待可并行完成。
- 启动期扩展欢迎页先即时清理，后续短时扫描移到后台，不再让每个环境固定多等待约 4 秒。
- 多环境启动仍保持有界并发，避免 10 个以上钱包扩展进程同时争抢磁盘、CPU 和内存。

## 扩展稳定性

- 启动前校验显式加载的解包扩展目录和 manifest；损坏、缺失或不受支持的扩展会被单独隔离并写入日志。
- 一个异常扩展不再阻塞或拖垮整批环境，Chrome Web Store 已安装扩展及现有用户数据不受影响。
- 扩展启动页后台清理增加超时与异常隔离，避免网络调试接口卡顿影响浏览器主启动流程。

## 同步助手

- Esc 恢复同步仍只显示 1 秒绿色提示。
- 完整同步窗口使用与状态模块一致的宽提示；紧凑助手使用紧凑提示；136×44 Logo 浮窗使用小字号、小范围提示。
- Esc 暂停保持静默，恢复后继续沿用原有主控、跟随窗口及同步参数。

## 升级兼容

- 在线更新只替换程序文件，不覆盖现有 data、Cookies、扩展、钱包、代理池、浏览器内核或激活数据。
- 支持从 v1.7.18–v1.7.30 直接升级到 v1.7.31，无需逐版本安装。
