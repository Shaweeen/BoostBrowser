# BrowserStudio v1.7.47

## 环境空白页启动修复

- 环境默认启动时只保留一个 `about:blank` 标签页。
- Rabby、Jupiter、Miden、MetaMask 等扩展仍保持安装、启用及原有钱包数据，但不再自动展示欢迎、引导、解锁或主页标签。
- 用户启动完成后主动点击扩展图标时，扩展页面正常打开，并继续遵循当前平铺、堆叠或横排区域的自适应约束。

## 启动逻辑

- 删除 v1.7.46 中重复执行的固定延时清理，并删除常驻式目标事件监听设计。
- 每次环境启动或重新启动只执行一次 Chrome 启动目标枚举，只处理无用户 opener 的顶层扩展启动标签，完成后立即断开。
- 不轮询窗口、不设置延时任务、不保留监听或状态表，也不修改第三方扩展文件、扩展存储或钱包数据。

## 升级兼容

- 可从 v1.7.46 或更早版本直接在线升级到 v1.7.47。
- 升级继续保留 `data`、环境编号、Cookies、浏览器账号、扩展及钱包数据、代理池、内核和激活状态。
