# BrowserStudio v1.7.48

## 环境启动标签修复

- 修复同一环境启动后出现两组钱包扩展标签的问题。
- Preferences 成为唯一的初始页来源；删除命令行重复加入的 `about:blank`，每个环境只保留一个空白标签。
- 环境窗口先以最小化状态完成启动准备，一次性关闭 Rabby、Jupiter、Miden、MetaMask 等扩展自动创建的顶层页面，再恢复并显示环境窗口，避免在前台逐个关闭标签。
- 启动期自动扩展标签即使带有 Chrome `openerId` 也会关闭；清理完成后立即断开，用户随后主动点击扩展不受影响。

## 资源与生命周期

- 删除每个环境四次重试的启动窗口尺寸后台线程、定时等待、取消状态表和排列阶段取消分支。
- 初始窗口尺寸改为启动收尾阶段的一次性设置，不新增监听、轮询、常驻控制或额外进程。
- 正常关闭的环境没有 Singleton 锁文件时，不再为每次启动创建 PowerShell 并扫描全系统进程；只有检测到锁文件时才执行安全占用检查。
- 删除每个运行环境每 2 秒读取一次全部标签页并写入数据库的旧恢复采集器；当前产品要求空白页启动，历史 `LastTabs` 数据仍保留用于旧数据兼容，但不再产生后台采集负载。
- 删除已无启动调用、却仍保留完整注册表与 CDP 定时器的旧窗口尺寸追踪器；历史窗口尺寸字段继续保留用于旧数据兼容，当前窗口仅由一次性启动模板和用户触发的同步排列管理。
- 删除从未返回任何参数的 Cloak 代理地理参数分支及其未接入运行链路的语言拼装代码，避免保留误导性的备用实现。
- 扩展、钱包存储、Cookies、登录状态、环境身份、代理及内核数据均保持不变。

## 升级

- 可从 v1.7.47 或更早版本直接在线升级到 v1.7.48。
- 在线升级继续保留现有 `data` 目录和全部用户环境数据。
