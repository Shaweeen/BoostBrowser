# BrowserStudio v1.7.73

## 热修：平铺 / 同步找不到窗口

- **回归修复（1.7.72）**：主窗口标签为钱包品牌（如 MetaMask）时，被误判为弹窗，导致平铺报「没有可用的运行实例窗口」、同步找不到主控窗。
- **分类改为尺寸优先**：大面积/平铺格子仍视为主窗；仅紧凑竖向通知窗按扩展弹窗处理。
- **HWND 回退链恢复**：主框解析失败时回退到进程树最大 Chrome 窗与会话句柄，避免硬失败。

## 含近期能力

- v1.7.72：10 窗平铺稳定性、链接钱包 CDP/CSS 坐标、二级弹窗进程树匹配。

## 升级说明

- 支持从 v1.7.72 及更早版本在线升级到 v1.7.73。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展包库、代理池、浏览器内核或激活数据。
