# BrowserStudio v1.7.32

## 扩展页面与用户数据

- 浏览器仍固定从空白页启动，但不再关闭扩展自动打开的初始化、解锁、连接钱包、授权或网络确认页面。
- 不再删除 Chrome 会话文件；Cookies、扩展数据、钱包状态和用户安装软件的网络访问逻辑保持原样。
- 当扩展有效页面已经打开时，仅关闭旁边多余的 `about:blank`/新标签页；没有扩展页面时保留唯一空白起始页。
- 对延迟约 1 秒才出现的扩展页面再执行一次轻量空白页检查，不建立长驻监听，也不阻塞浏览器启动。

## 钱包二级、三级弹窗

- 同步开启期间，扩展通知、连接、签名和授权弹窗固定约束在各自所属的环境窗口内部。
- Rabby、MetaMask 等钱包弹窗按 390×620 的正常扩展界面范围适配；当平铺单元更小时继续按可用区域缩小。
- 普通网页主窗口、DevTools、输入法窗口和 Chrome 主界面不会被误判为钱包弹窗。
- 二级、三级 Chrome 菜单仍沿用所属环境边界约束，不会覆盖相邻环境。

## 同步与性能

- 同步功能成功启动后，主 BrowserStudio 客户端立即最小化到任务栏；独立同步助手继续显示。
- 同步弹窗边界扫描从每 40–75 毫秒调整为 120–250 毫秒，窗口越多扫描频率越低，减少多环境窗口枚举和 CPU 占用。
- 保留 v1.7.31 的 2–4 路有界批量启动、调试接口并行等待和损坏扩展隔离逻辑。

## 升级兼容

- 在线更新只替换程序文件，不覆盖现有 data、Cookies、扩展、钱包、代理池、浏览器内核或激活数据。
- v1.7.18–v1.7.31 可直接升级到 v1.7.32，无需逐版本安装。
