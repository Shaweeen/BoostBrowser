# BrowserStudio v1.7.81

## P0：热启动零 CLI、保留用户 Session

- **完整性/热启动已确认**：启动时 **强制去掉全部 `--load-extension`**，不再二次注入扩展。
- **Session 清理**：仅在「仍需 CLI 首次适配」时丢弃可恢复标签文件；热启动 **保留用户上次网页/标签**，减少扩展反复弹窗与工作页丢失。

## P1：同步列表以客户端已启动环境为准

- **主路径**：扫描本机已启动 Chromium（`user-data-dir` + 调试端口），匹配主客户端配置的环境。
- **辅路径**：各环境 `DevToolsActivePort`；快照仅合并 HWND/PID 提示。
- **修复**：启动同步时 **不再** 用快照覆盖清空 live 扫描结果（此前会导致列表卡在约 30 个）。
- 目录名兜底匹配、HWND 批量解析 + 主框兜底、进程扫描 2s 缓存。

## 升级说明

- 支持从 v1.7.80 及更早版本在线升级到 v1.7.81。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展包库、代理池、浏览器内核或激活数据。
