# BrowserStudio v1.7.69

## 扩展方案 A（完整性冻结 + 同步询问）

- **数据完整即正常启动**：环境 user-data 中扩展已适配完整时，启动跳过安装、CLI 注入与重复验证。
- **完整性标记**（`.boost_extension_integrity_v1`）：验证通过后写入；分配变更时清除，仅在需要时再适配。
- **客户端打开一次巡检**：自动扫描全部环境扩展是否完整；完整后本会话不再重复巡检。
- **新建环境 / 批量创建**：若已有扩展包，询问是否同步到新环境。
- **添加 / 分配扩展**：询问是否同步到全部环境（取消则仅勾选环境）。
- 仍沿用 1.7.68 可靠策略：不整包拷贝；无 LES 时保留 `--load-extension`；有持久化 data 后跳过 CLI，减少扩展弹页。

## 含 1.7.68 修复

- 修复 1.7.67 无扩展、启动卡住问题（Prefs 假写入 + 整包拷贝）。

## 升级说明

- 支持从 v1.7.68 及更早版本在线升级到 v1.7.69。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展包库、代理池、浏览器内核或激活数据。
