# BrowserStudio v1.7.110

## 修复内容

- 已导入扩展的程序包改为保存在 `data/extensions/imported`。后续升级、修复安装或替换主程序不会再让环境指向安装目录中可能失效的扩展代码。
- 首次运行本版本时，会一次性从旧安装目录迁移有效扩展包；若旧共享包不存在，会仅从同一环境的 `Default/Extensions/<扩展ID>/<版本>` 恢复扩展程序代码。旧目录、Cookies、Local Extension Settings、IndexedDB 和钱包数据均不会删除或覆盖。
- 环境启动时仅在本次启动命令中使用迁移后的路径，不改写用户保存的启动参数，也不改变外部/手动安装扩展的路径。
- “全局分配”不再仅相信历史完成记录；每次用户主动点击分配时会核对当前环境是否仍有可加载的扩展包，缺失环境会被重新绑定，已有可用扩展不覆盖。
- Cloak/隔离内核按需启用本地 Chrome Web Store 桥接；普通 Google Chrome 不加载该桥接。桥接包保存在 `data/extensions`，不作为用户分配的业务扩展写入环境列表。

## 数据安全与兼容

- 不读取、收集、导出或修改助记词、私钥、钱包密码、Cookies、Local Extension Settings、IndexedDB 或账号登录状态。
- 不删除旧扩展目录；迁移只复制经 `manifest.json` 验证的扩展程序代码。
- 扩展包缺失且环境内也没有可恢复的程序代码时，会保留现有环境数据，不会用空包覆盖；用户可通过扩展管理的“分配”重新安装该扩展。

## 验收方式

1. 从旧版本更新后，检查 `data/extensions/imported` 是否出现已分配扩展的目录；旧 `extensions/imported` 目录仍应存在。
2. 打开已有环境，在 `chrome://extensions` 确认原有 Rabby、MetaMask 等扩展仍处于启用状态；钱包原有登录/数据不应被要求重新导入。
3. 新建环境后，在扩展管理中对可信扩展点击“分配”；只应添加缺失扩展，不应覆盖已有可用扩展。
4. 使用 Cloak/隔离内核打开 Chrome 网上应用店并尝试安装一个非钱包测试扩展；普通 Google Chrome 环境不应额外出现商店桥接扩展。
