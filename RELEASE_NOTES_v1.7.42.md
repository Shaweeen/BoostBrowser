# BrowserStudio v1.7.42

## 扩展分配与启动修复

- 扩展管理改为完全动作驱动：仅在用户点击“分配/全局执行”或创建继承全局选择的新环境时处理扩展；浏览器启动不读取扩展策略、不扫描扩展目录、不重新分配。
- 手动分配与全局执行会按扩展 ID、名称和来源检查目标环境，仅向缺少该扩展的环境添加启动配置，已有扩展及其数据不会被覆盖。
- 已下载且清单有效的扩展程序包会直接复用，不因再次点击分配而替换原目录。
- 用户执行分配时为目标环境打开扩展开发者模式，并移除会与已分配扩展冲突的 `--disable-extensions` 参数。
- 环境编辑会保留已经分配的扩展参数；全局选择只在创建新环境时继承，不在每次启动时后台补写。
- 移除分配只解除 BrowserStudio 写入的启动参数，不清理 Chrome 扩展设置、钱包存储或 Cookies。
- 删除旧的启动期扩展清理、清单扫描、自动固定和内核切换代码，减少启动 I/O 与隐式副作用。

## 数据兼容

- 在线升级继续保留现有 `data`、环境编号、Cookies、扩展、钱包数据、代理池、浏览器内核和激活状态。
- 新增的 `data/profile-extensions.json` 仅保存扩展 ID 与环境 ID 的分配关系，不保存钱包助记词、密码或扩展私有数据。
