# BrowserStudio v1.7.123
## 扩展管理：识别商城已安装，尊重用户版本
- 识别用户在 Google Chrome 商城中已安装的扩展（Chrome 内部托管条目）：BrowserStudio 扩展管理导入时不再重复下载、不再重复绑定到环境；所选环境已全部安装时弹出确认提示，点击“确定”跳过，不会覆盖用户已安装的版本。
- 启动环境时自动剔除“商城已原生安装”扩展的重复命令行注入（含已存在环境的遗留启动参数），修复 Rabby / MetaMask 等扩展在重启环境后重复弹出欢迎/解锁窗口的问题。
- 尊重用户自行安装的扩展：不改写其 Chromium `Preferences` 条目、不清除其程序包目录、不禁用它；BrowserStudio 只管理自己分配的扩展，商城安装的扩展保留自动更新。
- 启动清理不再误关用户主动打开的商城已安装扩展页面（如钱包解锁页）。
## 多环境与数据保护
- 扩展安装只保存经过验证的程序包和该环境的启动关联，不改写用户数据、登录状态、钱包或扩展存储；已有扩展及其原始扩展 ID 保持不变。
## 验证
- 商城安装 MetaMask / Rabby 后在 BrowserStudio 导入同款扩展：提示已安装并跳过，重启环境无重复弹窗、扩展不丢失。
- go build / go vet / go test、前端构建、打包测试全部通过。
