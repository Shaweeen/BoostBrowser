# BrowserStudio v1.7.33

## 稳定性与代码瘦身

- 删除已经停用且没有生产调用方的全局弹窗轮询、每环境弹窗轮询和离屏窗口恢复逻辑。
- 删除旧的 Service Worker DevTools 恢复监听，避免后台监听重复枚举窗口或意外移动用户窗口。
- 移除废弃环境开关、无调用兼容函数和仅服务于旧实现的测试代码。
- 本次源代码净减少 869 行，保留当前有效的窗口启动边界和同步弹窗约束逻辑。

## 可恢复的迭代审查

- 新增删除台账，记录删除位置、原始版本、删除原因、替代实现、验证方式和精确恢复方法。
- 新增发布前代码健康检查，自动统计源代码增删量和变化最大的文件。
- 大规模删除但未登记删除台账时停止发布，避免无法追踪的误删。
- 单次异常净增长需要明确的人工审查原因，防止长期堆积重复实现。
- 已淘汰的窗口监听入口重新出现时停止发布，避免历史问题回流。

## 功能兼容

- 继续使用 v1.7.32 的扩展初始化、钱包连接和授权页面保留逻辑。
- 继续使用同步期间的二级、三级钱包弹窗所属窗口约束。
- 本次更新不改变 data、Cookies、浏览器扩展、钱包数据、代理池、内核或激活状态。
- v1.7.18–v1.7.32 可直接在线升级到 v1.7.33，无需逐版本安装。
