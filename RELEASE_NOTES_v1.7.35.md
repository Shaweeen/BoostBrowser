# BrowserStudio v1.7.35

## Windows 打包稳定性

- 正式发布测试不再执行依赖外部示例节点的代理测速调试用例，避免网络超时导致 `go test ./...` 随机失败。
- 外部代理测速调试用例保留在源码中，并改为仅通过 `-tags=integration` 显式运行，没有删除测试能力。
- Windows 打包会将完整 Go 测试输出保存到 `build\go-test-windows.log`；若再次失败，可直接查看准确的失败包和测试名称。
- 正式测试增加 `-count=1`，避免 Go 测试缓存掩盖当前源码问题。

## 多窗口性能

- Windows 浏览器进程存活检测改用系统原生 API，不再为每个环境反复启动 `tasklist.exe`。
- 降低 20 个及更多环境运行时状态刷新、主控识别和同步列表更新的额外系统开销。

## 升级

- v1.7.18–v1.7.34 可直接在线升级到 v1.7.35，无需逐版本安装。
- 升级继续保留现有 `data`、环境编号、Cookies、扩展、钱包扩展数据、代理池、内核与激活状态。
