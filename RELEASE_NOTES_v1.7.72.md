# BrowserStudio v1.7.72

## 多开平铺 + 同步稳定性

- **10 窗平铺偶发错位**：每次平铺重新解析主浏览器框（不移动钱包/扩展弹窗）；`DeferWindowPos` 原子落位；多窗延长 layout-hold，避免与弹窗禁锢器半路抢位置。
- **格子顺序稳定**：环境编号自然排序（1,2,10…），主控固定左上；7–9 窗用 3×3，10+ 用 4 列；同步句柄平铺后刷新。
- **主框识别阈值**：适配小格尺寸，避免把主窗误判成弹窗。

## 链接钱包同步

- **Connect Wallet 跟随窗不响应**：主框点击优先同步到 https dapp 页，不再在焦点探测失败时误打进扩展文档。
- **DPI/缩放坐标**：CDP 鼠标使用 CSS 视口（`innerWidth/innerHeight`），右侧按钮在 125%/150% 缩放下也能点中。
- **二级钱包弹窗**：按进程树匹配扩展 Notification/确认窗；扩展表面优先扩展文档，主框仍走页面文档。

## 含近期能力

- v1.7.71：同步精度（渲染区点击、滚轮比例、扩展弹窗双候选）。
- v1.7.70：扩展只读检测取消重复 CLI load；批量启动按编号串行。

## 升级说明

- 支持从 v1.7.71 及更早版本在线升级到 v1.7.72。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展包库、代理池、浏览器内核或激活数据。
