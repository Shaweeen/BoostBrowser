# BrowserStudio v1.7.63

## 同步输入（钱包密码 / 授权 / 数字）

- 扩展页（解锁、弹层、授权）正确匹配主从 CDP 目标，密码与数字输入同步显示。
- 匹配失败时回退 Win32，不再静默丢键。
- 可编辑框状态按聚焦扩展页镜像（含 password / data-testid 定位）。
- 同步期间不镜像 `Ctrl+W` / `Ctrl+Shift+W` / `Ctrl+F4` / `Alt+F4`，避免从属标签或整窗被关掉。

## 多环境稳定性

- 用户接管收标签：**先**把保留页改成 `about:blank`，再关其它页；绝不先关最后一页（避免 Chromium 整窗退出）。
- 同步面板进程不再二次执行 handoff 收标签，避免与主进程竞态关页。
- 启动关闭扩展自动页时，若只剩扩展页会先建 blank 再关，防止环境窗口突然消失。

## 同步布局

- 平铺无缝网格（余数像素分配 + 1px 内侧重叠）。
- 主控前台限制放宽：同步面板置顶时主从点击/输入仍可同步。

## 升级说明

- 支持从 v1.7.62 及更早版本在线升级到 v1.7.63。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展、代理池、浏览器内核或激活数据。
