# BrowserStudio v1.7.121

## 创建环境不再联动扩展中心

- 单个创建和批量创建环境后，不再读取扩展程序中心的插件列表。
- 不再弹出“是否同步已有扩展到新环境”的提醒。
- 创建、忽略旧数据恢复后，不再自动分配或安装扩展中心插件。
- 新环境默认保持独立；需要插件时，由用户进入扩展程序中心、选择目标环境并主动点击“分配”。
- 已有环境的扩展分配、Cookies、钱包及扩展存储不受影响。

## 验证

- 单个和批量创建均不会继承全局扩展包。
- 创建页面不再引用扩展包读取、提醒或同步接口。
