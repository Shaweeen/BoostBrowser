# BrowserStudio v1.7.37

## 同步窗口采集状态机

- 同步助手取消每秒轮询、窗口聚焦刷新、页面可见性刷新及浏览器生命周期事件自动刷新。
- 窗口采集改为操作触发：首次打开助手、手动刷新、全选、设定主控及开始同步时读取当前已打开环境。
- 开始同步由后端再次执行一次权威进程与窗口核对，避免使用旧 PID、旧窗口句柄或已经关闭的环境。
- 停止同步、进入重新配置或退出同步助手时，释放本次环境列表、选择项、主控和同步显示状态。
- 显式重新采集后，已关闭或不存在顶层窗口的环境会从选择和主控状态中移除。
- 全选按钮在采集数据已释放的空状态下仍可使用，点击后会先扫描全部当前窗口再执行全选。

## 稳定性与资源占用

- 删除异步二次发现与自动轮询交错更新，避免旧扫描结果覆盖用户刚完成的新配置。
- 闲置同步助手不再持续扫描进程树和窗口句柄，减少多环境运行时的后台资源消耗。
- 保持现有输入同步、Esc 暂停/恢复、窗口排列和扩展弹窗同步行为不变。

## 升级

- v1.7.18–v1.7.36 可直接在线升级到 v1.7.37，无需逐版本安装。
- 升级继续保留现有 `data`、环境编号、Cookies、扩展、钱包扩展数据、代理池、内核与激活状态。
