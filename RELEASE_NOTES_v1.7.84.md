# BrowserStudio v1.7.84

## P0：CloakBrowser 内置隔离内核反自动化修复

- **移除 `--test-type` 自动化测试标志**：X（Twitter）等严格反自动化站点会把它与浏览器身份异常关联，导致「页面无法完整加载 / 登录提醒 / 临时登录限制」。CloakBrowser 内核已在源码层消除 infobar，不再需要该标志的压制作用；`--disable-infobars` 仍保留用于压住 `--no-sandbox` 黄条。
- 非 cloak 内核路径与用户显式配置的 `--test-type` 行为不变。

## P1：环境详情页真实标签页数据

- **「打开」按钮真实导航**：`BrowserInstanceOpenUrl` 之前只返回成功、实际不打开任何页面；现通过 CDP 导航（cloak 内核走无注入路径，其余内核走 blank→注入→navigate 路径）真实打开目标 URL。
- **「标签页列表」返回真实数据**：`BrowserInstanceGetTabs` 之前返回编造的示例标签（"示例站点 example.com"）；现读取 DevTools `/json` 的真实页面目标，未运行或读取失败时返回空列表。

## P2：缓存清理改为用户控制

- **默认不再自动清理**：自动清理默认关闭（含旧配置迁移逻辑不再强制开启）；已显式开启过的用户保留其选择。
- **周期最低 30 天**：删除每周 7 天选项；开启后可选 30（每月）/ 60 / 90 天，历史小于 30 天的配置自动提升到 30 天。
- **设置页新增「立即清理」按钮**：一键扫描并清理所有已关闭环境的可再生缓存，跳过运行中环境，保留 Cookies / 登录状态 / IndexedDB / 钱包扩展数据。

## P3：同步工具实时发现新环境

- **同步进行中也能自动发现新环境**：之前同步会话进行时列表不再自动刷新，新打开/重开的环境不会出现在「添加更多跟随环境」；现在同步期间以 10 秒低频静默刷新（空闲时 3 秒），新环境无需手动刷新即可添加为跟随并参与排列（横排/平铺/堆叠）。
- 关闭后重开的环境通过实时进程扫描 + 主客户端运行时快照自动修复会话目标，无需重启同步。

## Bug Fixes

- 修复 `BrowserInstanceGetTabs` 返回占位假数据、`BrowserInstanceOpenUrl` 假成功的问题。
- 修复缓存自动清理周期固定 7 天、无法按月设置的问题。
- 修复设置页无手动清理入口的问题。
