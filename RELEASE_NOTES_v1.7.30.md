# BrowserStudio v1.7.30

## 代理导入自动解析

- 批量输入支持空格、Tab、冒号、逗号、竖线和分号分隔，不再要求固定格式。
- 支持 `IP 端口 账号 密码`、`IP:端口:账号:密码`、`账号:密码@IP:端口`、标准 HTTP/HTTPS/SOCKS5 URL、域名和 IPv6。
- 未填写协议时使用导入窗口中选择的 HTTP、HTTPS 或 SOCKS5；`socks://`、`socket://` 自动规范为 `socks5://`。
- 自动编码账号和密码中的 `@`、`#`、冒号、斜杠及空格，避免认证信息被错误解析成 URL 地址。
- 每行独立验证地址和 1–65535 端口，错误信息会标明具体行号。

## HTTP / SOCKS5 稳定性

- 新导入和旧数据库代理统一在保存、启动、测速与 IP 健康检测时规范化，旧式代理文本无需重新录入。
- 修复后端测速和转发未解码 URL 认证信息的问题，包含特殊字符的账号密码可正常认证。
- 带认证的本地代理不再绕过本地转发，避免 Chrome 无法直接处理代理认证导致无网络。
- 标准代理协议探测结果缓存 10 分钟；多个环境复用同一代理时不再重复探测。
- 修复同一环境重复启动导致本地转发引用计数累积的问题。
- 代理测速必须真正取得互联网 HTTP 响应才判定可用；仅端口开放但认证失败或无网络不再显示绿色延迟。
- 限制坏代理的检测总等待时间，避免单个失效节点长时间阻塞批量检测。

## 数据与升级兼容

- 在线更新只替换程序文件，不覆盖现有 data、Cookies、扩展、钱包、浏览器内核或激活数据。
- 保留 v1.7.29 的全局 Esc 同步暂停与恢复功能。
