# BrowserStudio v1.7.71

## 多环境同步精度

- **默认立即同步**（随机延时仍为可选）；启动同步时强制回到立即模式。
- **页面内点击**优先按 Chrome 渲染控件映射，同尺寸 1:1，异尺寸按比例。
- **滚轮**：落点与点击同一套校准；异尺寸时横向按宽、纵向按高缩放滚动量。
- **扩展/钱包弹窗**：相对偏移 + 贴边双候选匹配；匹配失败回退主窗，避免点击丢失。
- 不改动同步会话模型，仅修正坐标与滚动路径。

## 含近期能力

- v1.7.70：扩展只读检测取消重复 CLI load；批量启动按编号串行。
- v1.7.69：完整性标记与新建/分配时的扩展同步询问。

## 升级说明

- 支持从 v1.7.70 及更早版本在线升级到 v1.7.71。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展包库、代理池、浏览器内核或激活数据。
