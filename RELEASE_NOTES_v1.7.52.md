# BrowserStudio v1.7.52

## 扩展启动页面修复

- 每一次环境进程启动，在浏览器仍处于最小化且调试端口刚稳定时立即执行唯一一次标签快照；关闭并重新打开环境会执行新进程自己的这一次清理。
- 保留浏览器内核创建的唯一空白页，仅关闭本次启动时扩展自动打开的顶层页面和多余空白页。
- 清理发生在任何显式页面导航和用户交付之前；完成启动后，用户、网页或应用主动拉起的钱包扩展页面不参与清理，可正常打开和使用。
- 显式配置的启动 URL 在清理结束后才打开，因此不会被启动清理关闭。
- 调试接口延迟就绪时不再补做标签清理，避免浏览器交给用户后误关正常页面。
- 不修改扩展代码、扩展启用状态或钱包存储，也不增加后台监听、轮询、定时器或常驻清理任务。
- 清理只读取 CDP 标签的 ID、类型、URL 和 opener 归属，不读取 DOM、表单、密码、私钥或助记词，也不保存这些信息。

## 升级说明

- 支持从 v1.7.51 及更早版本直接在线升级到 v1.7.52。
- 升级不会覆盖现有环境、Cookie、钱包、扩展、代理池或激活数据。
