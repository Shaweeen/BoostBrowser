# BrowserStudio v1.7.75

## 同步滚动（MetaMask 等扩展全页）

- 主控在 **扩展页**（如 MetaMask home）上下滚动时，跟随窗同步滚动。
- 按 **当前可见标签**（`visibilityState`）选择 CDP 文档，不再误选后台 https 页。
- 扩展 ID 软匹配 + 滚轮匹配失败时回退 Win32，避免滚动被静默丢弃。

## 平铺间隙

- 窗口上下/左右间隙改为 **0**（贴齐；Win32 无法做 0.15px 亚像素间隙）。
- 所有窗口仍保持相同宽高。

## 升级说明

- 支持从 v1.7.74 及更早版本在线升级到 v1.7.75。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展包库、代理池、浏览器内核或激活数据。
