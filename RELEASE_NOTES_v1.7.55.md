# BrowserStudio v1.7.55

## 多开环境与钱包扩展

- 修复通过 `--load-extension` 安装的钱包扩展偶发读不到环境内钱包数据：解包 CRX 时写入稳定公钥 `key`，启动前自动修复已装扩展包。
- 多开环境时（无需打开同步助手），钱包/扩展/二级窗口约束在各自环境区域内；保留自然尺寸，超出格子时等比例缩小。
- 环境启动标签收敛：延迟出现的扩展主页也会关闭，尽量只保留一个空白起始页。

## 同步与输入

- 优化键鼠、滚轮、Ctrl+滚轮缩放、Shift+横向滚动与中文输入法同步手感（功能路径不变）。
- 同步助手面板补充操作提示与延迟文案（即时 / 轻抖动 / 随机）。

## 备份与数据

- 导入备份后对齐 Profile ID 与用户数据目录指针，列表自动刷新便于检查编辑。
- 新建环境立即建立数据目录与身份指针；关闭写盘确认轮询更密、仍不强制杀进程。

## 性能与界面瘦身

- 去掉环境列表 2 秒轮询、主窗边界 1.5 秒轮询、日志页 3 秒轮询；改为事件/可见时刷新。
- 侧栏按日常/配置/帮助分层；删除 charts 脚手架与空 AdminKeygen 页面；清理误提交的 backend UUID cookie 暂存目录。

## 升级说明

- 支持从 v1.7.54 及更早版本直接在线升级到 v1.7.55。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展、代理池、浏览器内核或激活数据。
