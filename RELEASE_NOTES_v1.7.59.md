# BrowserStudio v1.7.59

## 启动与扩展

- 扩展分配后：首次打开做非破坏性补全与校验（包 key / 空目录脚手架），对齐后写入就绪标记；后续启动跳过扩展包扫描与 Preferences 整扫。
- **禁止覆盖**已有 Local Extension Settings、Preferences.extensions.settings、Cookie、钱包与社媒状态。
- 启动标签：仅在 debug 就绪时**单次**关闭扩展自动页，无后台长轮询、无实时监听，避免误关用户主动打开的扩展页。

## 窗口与同步

- 仅在用户点击启动环境时，给**环境主窗口**定一次默认 1400×600；不固定弹窗/扩展尺寸。
- 同步进行中不套默认主窗尺寸；平铺/堆叠完全按用户排列与弹窗约束规则。

## 升级说明

- 支持从 v1.7.58 及更早版本在线升级到 v1.7.59。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展、代理池、浏览器内核或激活数据。
