# BrowserStudio v1.7.62

## 标签页管理（每轮启动完整流程）

- **地基**：Preferences 固定 `restore_on_startup=4` + `startup_urls=["about:blank"]`，浏览器默认只开空白页。
- **启动时**：debug 就绪关闭扩展自动页；每启动/停止环境会重新武装周期。
- **用户接管（最后一次）**：点击同步工具、主客户端环境列表或同步面板时，所有运行环境只保留一个 `about:blank`（newtab 就地改为 blank），随后完全由用户接管。
- **停止后再开**：整套流程完整重跑；接管后不再做任何标签管理。

## 升级说明

- 支持从 v1.7.61 及更早版本在线升级到 v1.7.62。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展、代理池、浏览器内核或激活数据。
