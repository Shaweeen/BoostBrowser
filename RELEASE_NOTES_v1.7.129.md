# v1.7.129 发布说明

## 代理池测速 / IP 健康检测与网络模式对齐 + TUN 接管不再依赖 Chrome 代理

### 修复的问题

用户在使用本地 VPN（Clash 等，TUN 模式）时，代理池里全部节点显示「超时/不可用」，
环境里访问网页慢、部分页面加载失败（X 登录 "Something went wrong"、Discord 登录卡加载）。

**根因（审计结论）：**

1. **不是端口冲突**。应用的所有本地监听端口（relay、调试端口、xray/sing-box 桥接）
   都是动态分配（127.0.0.1:0），唯一固定端口是 LaunchServer 19876（仅启动码功能，
   且占用时只记日志、不致命）。
2. **真正的回路问题**：代理池测速 / IP 健康检测 / 环境 relay 到代理节点的连接**永远
   直连拨号**。本地 Clash 开 TUN 时，这些直连会被 TUN 截获、经 Clash 的节点转发，
   而 Clash 节点出口 IP 通常连不上代理服务器（SOCKS 端口白名单等）→ 全部超时；
   环境内「部分连接成功、部分连接失败」也正是同一条连接路径的随机性。

### 本次改动

| 模块 | 改动 |
|---|---|
| **代理池测速**（`SpeedTest`） | 拨号路径与设置的网络模式一致：`local_gateway` 经本地 VPN 网关（Clash 7897 等）拨号；`auto` 直连优先、失败自动回退网关；`tun`/`direct` 直连（TUN 在系统层接管）。池页显示的可用性/延迟与环境的真实路径一致，不再误报 |
| **IP 健康检测**（`FetchIPPureInfo`） | 同样的网关感知拨号：`local_gateway` 经网关、`auto` 直连失败回退网关 |
| **TUN 接管模式**（环境启动） | 选择「TUN 接管」后，环境**不再配置 `--proxy-server`、不再启动本地 relay**——流量全部交给本地 VPN(TUN) 在系统层路由，完全脱离 Chrome 代理模块，杜绝双层代理与网络回路。代理池节点需导入本地 Clash 并在其中选择；或改用「本地 VPN 网关（非 TUN）」模式让环境按原路径走代理池节点 |
| **回归测试** | 新增 5 个：http 节点经网关测速、socks5 节点经网关测速、网关不可用明确报错、auto 直连、IP 健康客户端经网关；并收录慢拨号节点测速测试（慢节点不再误判超时） |

### 使用建议

- **用 Clash + 代理池节点**：设置 → 代理网络模式选「本地 VPN 网关（非 TUN）」，
  网关填 `127.0.0.1:7897`（或自动探测）。环境与池页都经 Clash 连到节点。
- **用 Clash TUN 全接管**：模式选「TUN 接管」，节点导入 Clash 并在 Clash 里选择；
  环境完全不依赖 Chrome 代理模块。
- 模式保持「自动」也能工作：直连失败会自动回退本地网关。
