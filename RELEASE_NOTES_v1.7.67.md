# BrowserStudio v1.7.67

## 扩展启动（方案 A：装进环境 Profile）

- 分配扩展时写入环境 `Default/Extensions` + Preferences，**日常启动不再依赖 `--load-extension`**，从根上减少解锁/home 全页进标签栏。
- 启动时对已分配包做 Profile 安装/再启用；安装失败才回退单次命令行加载。
- **解绑**：从分配列表移除，并在 Profile 中禁用扩展（**保留钱包 LES/Cookies**）；再分配可恢复启用。
- 运行中解绑：下次冷启动自动对账，禁用已不在分配列表中的 Profile 扩展。
- 扩展版本更新时切换 Preferences 指向新版本目录。
- 启动固定 `about:blank` 会话基础，并丢弃可恢复的标签 Session 快照，避免「上次未关的扩展页 + 再次激活」叠成双标签。

## 钱包弹窗

- `*Notification*` 类窗口**不强制改尺寸**；打开后 2 秒宽限再考虑 force-fit，减少 Rabby 等白屏/抖动。
- 多开时减少纯 Z 序 `SetWindowPos` 打断扩展首绘。

## 同步

- 同步启动只建立 URL 基线，**不对从机整页 `Page.navigate`**。
- 从机已在同一地址时跳过导航，降低断钱包连接、需清 Cookie 才能重连的情况。

## 代理

- 环境列表/卡片：绿色拨动开关（代理正常 / 代理暂停）；绑定保留，暂停后启动走本机直连。

## 升级说明

- 支持从 v1.7.66 及更早版本在线升级到 v1.7.67。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展包库、代理池、浏览器内核或激活数据。
- 老环境首次用本版本启动时会自动把已分配包装进 Profile；之后启动应不再重复弹出扩展全页标签。
