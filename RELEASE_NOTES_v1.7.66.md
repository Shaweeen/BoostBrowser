# BrowserStudio v1.7.66

## 扩展启动（根治 --load-extension 重注）

- 扩展注入改为**按需 / 一次性适配**：环境数据已就绪时热启动不再重复 `--load-extension`，从根上减少扩展自动开页与启动抖动。
- 钱包/扩展数据已存在且热稳定时，跳过重复注入路径，启动更干净。
- **移除**启动后 CDP 关标签补丁（CLEAN-063）：不再用事后清标签掩盖注入策略问题。

## 输入同步

- 默认同步为**立即同步**；随机延时改为可选，释放同步会话时延时预设会复位为关闭。

## 弹窗与指纹

- 钱包/扩展弹窗在平铺格内**强制适配**，几何由约束器统一写入，减少缩放闪烁。
- 启动路径加强指纹锁定与 sole-blank 交接基础；同步路径与校准逻辑对齐。

## 代理

- 代理解析更稳（含 `user:pass:host:port` / 主机端口字段顺序）；粘性探测缓存与中继 keep-alive 更可靠。
- 完整连通性检测与出口地理/时区提示；智能分配可选**按代理时区对齐**。
- 代理池展示协议与出口标记；支持**仅重测失败项**。
- **按环境临时暂停代理**：绑定保留，暂停后启动走本机直连（`direct://`），下次启动生效。
- 环境列表 / 卡片改为**绿色拨动开关**：默认「代理正常」，关闭显示「代理暂停」，再开恢复「代理正常」。

## 升级说明

- 支持从 v1.7.65 及更早版本在线升级到 v1.7.66。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展、代理池、浏览器内核或激活数据。
