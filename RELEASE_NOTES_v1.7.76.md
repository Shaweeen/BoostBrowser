# BrowserStudio v1.7.76

## 同步一次接管 + 启动加速

- **点击启动同步时（仅一次）**：主控与所有跟随环境收拢为 **唯一 `about:blank`**（先导航保留页再关兄弟页，避免关最后一页导致整窗退出）。
- **此后不再管理标签**：用户可自由打开扩展、新建网页标签；启动路径不做 CDP 关页扫尾。
- **启动仍轻量**：仅 Preferences 固定 about:blank + 丢弃可恢复 Session 文件（不动钱包/LES）；无多轮 post-start 检查。

## 升级说明

- 支持从 v1.7.75 及更早版本在线升级到 v1.7.76。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展包库、代理池、浏览器内核或激活数据。
