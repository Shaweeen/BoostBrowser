# BrowserStudio v1.7.80

## 对齐商业多开（AdsPower / MoreLogin 思路）

### 扩展如何“评定 / 导入 / 启动”
- **评定**：以环境 Profile 内是否已有扩展注册 + 持久化证据（Local Extension Settings 等）为准；有则日常启动 **取消 `--load-extension`**，避免每次激活弹解锁/引导页。
- **导入**：绑定到环境时写入 Profile（方案 A）；钱包/Cookies/LES **不覆盖**。
- **启动弹窗**：热启动（已适配）**不再清空**用户上次的标签会话；仅首次适配仍需 CLI 时才清理可恢复 Session，防止双扩展标签。

### 同步不再关用户工作页
- **取消**「点同步 → 强制只留 about:blank」自动行为（此前会在非同步使用后、再同步时关掉已有工作标签）。
- 同步 = **键鼠输入同步**；用户开的网页、扩展页、新标签一律保留。
- 同步工具环境列表：**扫描客户端已启动进程**（user-data-dir + 调试端口）为主，不单靠某一种快照文件。

## 升级说明

- 支持从 v1.7.79 及更早版本在线升级到 v1.7.80。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展包库、代理池、浏览器内核或激活数据。
