# BrowserStudio v1.7.38

## 代理池响应速度

- 添加、编辑和删除单个代理改为单条数据库操作，不再清空并重写整个代理池。
- 批量删除与完整导入使用单一 SQLite 事务，代理数量增加时仍保持稳定响应。
- 删除按钮先更新界面；持久化失败会自动恢复原列表，环境代理绑定核对在后台完成。
- 分组列表直接由当前代理数据生成，不再让保存按钮等待额外数据库查询。

## 协议识别与网络验证

- HTTP、HTTPS、SOCKS5 候选协议改为并发识别，首个真实互联网响应成功后立即返回。
- 单个候选协议使用 6 秒总预算，每个连通性站点最多等待 3 秒，避免错误代理长期卡住界面。
- 使用多个独立连通性端点作为快速回退，同时保留“TCP 端口可连不等于代理可上网”的判断。
- HTTP 407 代理认证失败不再显示为可用代理。
- 保存代理与网络验证完全解耦；用户可先完成编辑，再按需执行协议识别或批量测速。

## 升级

- v1.7.18–v1.7.37 可直接在线升级到 v1.7.38，无需逐版本安装。
- 升级继续保留现有 `data`、环境编号、Cookies、扩展、钱包扩展数据、代理池、内核与激活状态。
