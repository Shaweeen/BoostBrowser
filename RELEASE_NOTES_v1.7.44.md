# BrowserStudio v1.7.44

## 同步生命周期与稳定性

- 停止同步会等待输入、网址和弹窗工作线程完整退出，再释放本次会话的窗口数据。
- 手动刷新从存活窗口重新生成一份原子快照，避免混用不同 Start/Stop 周期的数据。
- “全选”和“设为主控”只操作当前快照，不再重复触发窗口扫描；开始同步前执行最终权威校验。
- 鼠标 Hook 对扩展弹窗改为有序异步投递，避免慢窗口阻塞主控输入。
- URL/CDP 操作按环境串行化，并使用不可变的会话停止信号，减少并发竞争和停止延迟。
- 排列窗口期间暂停坐标分发，防止窗口移动与鼠标、键盘同步交叉执行。

## 窗口与扩展弹窗

- 1400×600 仅作为环境首次启动的尺寸模板；用户执行平铺、堆叠或横排后立即取消启动模板约束。
- 二级、三级菜单和扩展弹窗按照所属环境当前窗口范围约束，不套用启动尺寸。
- 钱包弹窗宽度和高度分别限制，不再按固定宽高比缩放；减少无效空白区域并保留完整可滚动内容。
- 支持通过 Chrome 子进程树识别 Rabby、MetaMask、Jupiter 等钱包通知窗口的所属环境。

## 代码瘦身与发布门禁

- 删除无调用的旧版同步 localhost HTTP Bridge，统一使用当前共享运行时快照。
- 删除启动、备份、同步、扩展、钱包、代理和图标模块中的无调用私有辅助函数。
- 合并重复的钱包弹窗标题判断，保留现有产品识别范围。
- 新增项目级变更规范：禁止叠加新旧补丁逻辑，要求同模块审查、可恢复删除台账和完整回归验证。
- 删除达到门限或删除文件但未记录恢复台账时，发布流程会直接终止。

## 升级兼容

- 可从现有版本直接在线升级到 v1.7.44，无需安装中间版本。
- 升级继续保留 `data`、环境编号、Cookies、浏览器账号、扩展及钱包数据、代理池、内核和激活状态。
