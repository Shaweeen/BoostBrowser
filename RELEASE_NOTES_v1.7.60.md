# BrowserStudio v1.7.60

## 启动速度与标签

- 批量启动固定并发 **5** 路。
- 启动热路径对齐 1.7.4x：就绪后跳过 Preferences 整扫、书签合并、阻塞式扩展修包、非 Cloak stealth。
- 空白标签完全由浏览器默认提供；客户端**不创建、不关闭**空白页，仅单次关闭扩展自动打开的页面。
- 无长轮询 / 实时标签监听，避免误关用户主动打开的扩展页。

## 扩展与数据安全

- 首次对齐后写就绪标记；无扩展环境用 `.boost_start_prep_done` 避免每次重扫。
- 启动后异步补全扩展包时**禁止改写 Preferences**（防与 Chrome 并发损坏钱包元数据）。
- 不覆盖已有 Local Extension Settings / extensions.settings 用户数据。

## 安全加固

- 内核下载名称路径消毒，安装目录必须落在 `chrome/` 下。

## 窗口与同步

- 仅用户点击启动时对**主窗口**定一次 1400×600；弹窗/扩展尺寸不固定。
- 同步排列完全按用户设定与约束规则。

## 升级说明

- 支持从 v1.7.59 及更早版本在线升级到 v1.7.60。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展、代理池、浏览器内核或激活数据。
