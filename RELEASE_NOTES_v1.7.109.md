# BrowserStudio v1.7.109

## 修复内容

- 同步助手的平铺、堆叠和横排现在可以恢复已最小化到任务栏的环境主窗口，再执行排列；不会再因最小化被识别为“无窗口”。
- 最小化状态下仍严格排除钱包确认、OAuth 与其他拥有者窗口，避免把扩展弹窗误作为环境主窗口。
- 自动代理链路调整为：先测试环境独立代理；仅在该链路失败时才回退本地 VPN 网关。显式选择“本地 VPN 网关（非 TUN）”时仍遵循用户选择的双层链路。
- TUN 接管模式不再由自动策略额外叠加本地 VPN 网关，减少重复转发、潜在回环、延迟和掉线。

## 数据安全与兼容

- 不修改 Windows 系统代理、Clash Verge、VPN 或 TUN 配置。
- 不覆盖现有环境编号、浏览器文件、Cookies、扩展、钱包、IndexedDB 或 Local Extension Settings。
- 保留此前的扩展安全分配、环境数据归档与恢复链路：已存在的扩展及钱包数据不被重新覆盖。

## 验收方式

1. 启动多个环境并最小化或按 Win+D 显示桌面。
2. 打开同步助手，点击“刷新”后点击“平铺 / 堆叠 / 横排”。
3. 所有已启动环境应从任务栏恢复并按所选方式排列；钱包弹窗不应替代环境主窗口。
4. 员工依赖 VPN 时，启用 Clash Verge TUN 并在客户端选择“TUN 接管”；非 TUN 模式则显式选择“本地 VPN 网关（非 TUN）”。
