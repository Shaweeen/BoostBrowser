# BrowserStudio v1.7.65

## 多环境同步与校准

- 同步校准采用 Chrome-Manager 风格：主从窗口映射更稳，键鼠命中从属环境更准。
- Esc 仍只暂停/恢复输入同步；暂停与恢复不会改网址、刷新页面或覆盖表单。
- 钱包密码/验证码数字输入：弹窗匹配与焦点写入更可靠，减少从属环境丢字。

## 标签页交接（handoff）

- 交接改为**按环境（profile）隔离**：新开一批环境时，不会关掉其它已在工作中的标签页。
- 收拢到空白页仍遵守「先导航 about:blank、再关兄弟标签」规则，避免关最后一页把整窗打掉。

## 平铺排列

- 格子尺寸统一，**末行不满也不再横向拉长**，避免坐标映射错位导致同步点击偏。
- 无缝叠边逻辑保留；平铺/堆叠/横排切换时由统一布局持有期协调窗口几何。

## 钱包 / 扩展弹窗

- 弹窗几何**单一写入者**：只由环境弹窗约束器负责位置；同步路径只纠位置、不改尺寸。
- 平铺时进入 layout-hold，避免双路 `SetWindowPos` 互相打架导致钱包窗反复缩放闪烁。
- 扩展管理：**「移除」** 使用明确按钮 + 二次确认，避免三点菜单误删。

## 列表与批量启动

- 环境列表「更新时间」展示**最近一次关闭/保存**时间（`last_stop_at`）。
- 批量启动：自然数序号排序、并发提速；启动后可自动进入平铺。

## 代理池

- 测速/探测优先使用声明的 **SOCKS5** 协议，减少误判 HTTP。
- 智能分配：未绑定代理的环境可批量智能指派，交互更清晰。

## 升级说明

- 支持从 v1.7.64 及更早版本在线升级到 v1.7.65。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展、代理池、浏览器内核或激活数据。
