# BrowserStudio v1.7.49

## 唯一空白启动页

- 修复 v1.7.48 同时由 Chrome 初始 target 和 Preferences `startup_urls` 创建空白页，导致一个环境出现两个 `about:blank` 的根因。
- Preferences 不再配置启动 URL；旧版本遗留在环境启动参数里的空白页、网页或扩展页也不再参与启动。
- 进程命令行成为默认初始页的唯一所有者，并且只加入一次 `about:blank`；用户明确指定的启动网址仍由独立的网址导航流程处理。
- 扩展仍保持启用，用户点击工具栏扩展时可以正常打开。
- 启动阶段只在已分配扩展加载完成前执行有上限的同步就绪检查；检测到扩展后留出一次 500 毫秒完成窗口，随后关闭扩展自动创建的顶层标签并立即释放连接。

## 生命周期与数据安全

- 不增加常驻监听、后台轮询、标签合并、目标注册表或额外进程；启动就绪检查最长 2 秒，正常完成后立即退出。
- 不修改 MetaMask、Rabby、Jupiter、Miden 等第三方钱包扩展源码，也不伪造钱包初始化状态。
- 扩展本体、钱包存储、Cookies、登录状态、环境编号、代理、内核和现有 `data` 目录保持不变。

## 升级

- 可从 v1.7.48 或更早版本直接在线升级到 v1.7.49。
- 在线升级不覆盖现有环境和浏览器数据。
