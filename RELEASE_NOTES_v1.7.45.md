# BrowserStudio v1.7.45

## 同步助手窗口状态修复

- 主客户端环境列表成为唯一的运行状态权威来源。
- 打开同步助手前，主客户端发布当前运行环境的 ID、名称、PID、调试端口、HWND 和窗口 PID。
- 同步助手首次打开、手动刷新以及开始同步时，只读取主客户端发布的当前运行快照。
- 删除同步助手进程中的 Chromium 独立发现、运行状态修复和本地 PID 覆盖逻辑。
- 修复环境在主客户端显示“运行中”，但同步助手得到 PID 后仍全部显示“无窗口”的问题。
- 环境启动、停止或由主客户端重新接管后，会同步更新共享运行快照。

## 反复操作生命周期

- 停止同步只释放 Hook、工作线程、主控及跟随关系，不修改主客户端环境运行状态。
- 停止后再次点击刷新，会重新读取主客户端当前所有运行环境。
- 全选、取消全选、设定主控、排列和开始同步均继承最近一次刷新得到的窗口数据。
- 主客户端快照为空时清除同步助手旧状态，避免已关闭环境继续残留。
- 快照文件切换期间增加短暂读取重试，减少并发更新造成的瞬时空列表。

## 升级兼容

- 可从 v1.7.44 或更早版本直接在线升级到 v1.7.45。
- 升级继续保留 `data`、环境编号、Cookies、浏览器账号、扩展及钱包数据、代理池、内核和激活状态。
