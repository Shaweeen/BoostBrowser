# BrowserStudio v1.7.78

## 同步工具：大规模多开列表

- 以主客户端 `browser-runtime.json` 为权威运行列表，避免助手卡在约 30 个环境。
- 刷新时对所有 PID **重新解析窗口**；进程存活判断放宽（HWND 仍有效也算运行中）。
- 快照里有、面板尚未加载配置的环境也会显示。
- 大批量启动后发布快照前增加 settle 时间，减少「无窗口」。

## 钱包二级弹窗与任务栏

- 弹窗最终限制在 **工作区（不含任务栏）** 内。
- 底行环境打开的钱包/扩展弹窗会整体上移，避免被任务栏挡住无法点击输入。

## 升级说明

- 支持从 v1.7.77 及更早版本在线升级到 v1.7.78。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展包库、代理池、浏览器内核或激活数据。
