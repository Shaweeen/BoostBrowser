# BrowserStudio v1.7.39

## 代理协议兼容与自动修正

- HTTP、HTTPS、SOCKS5 验证成功后，将实际可用协议安全写回代理池；环境启动不再继续使用导入时标错的协议。
- 写回采用原配置比较保护，后台测速不会覆盖用户刚完成的新编辑。
- 单条测速、批量测速及导入后的后台测速都同步更新前端协议显示。

## 浏览器代理与 DNS

- 所有外部标准代理统一经过本地兼容转发，使测速、认证、DNS 与浏览器真实访问使用同一条链路。
- 目标域名通过 HTTP CONNECT 或 SOCKS5 域名地址交给上游代理解析，避免本地 DNS 污染和 DNS 泄漏造成的假断网。
- 兼容 Chromium 不读取命令行代理 URL 内账号密码、且不支持 SOCKS5 账号密码认证的限制。
- 本机 `127.0.0.1`、`localhost`、`::1` 的 Clash/VPN 代理保持直接连接，避免本地转发环路。
- 外部代理与 VPN TUN 同时使用时，如供应商 IP 被 TUN 再次代理，错误信息继续提示将代理服务器 IP 加入 TUN 路由排除。

## 升级

- v1.7.18–v1.7.38 可直接在线升级到 v1.7.39，无需逐版本安装。
- 升级继续保留现有 `data`、环境编号、Cookies、扩展、钱包扩展数据、代理池、内核与激活状态。
