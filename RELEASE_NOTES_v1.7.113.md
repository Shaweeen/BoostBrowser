# BrowserStudio v1.7.113

## 修复内容

- 修复 Windows 发布脚本对修复升级脚本版本化时的错误判定：当脚本默认目标版本已经等于当前发布标签时，现会正确识别为匹配并继续生成发布资产，不再在安装包已经构建完成后中断发布。
- 延续 `v1.7.112` 的扩展兼容与数据保护修复：不改写 Chromium 扩展登记，不覆盖用户自行安装的扩展、钱包、Cookies、浏览器登录状态或扩展本地数据。

## 验收方式

1. Windows 发布完成后，Release 资产中应包含 `BrowserStudio-Repair-Upgrade-v1.7.113.ps1`。
2. 更新后打开已有环境，在 `chrome://extensions` 确认用户已有扩展可见且启用。
