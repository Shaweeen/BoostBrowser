# BrowserStudio v1.7.43

## 安全数据清理

- 自动清理改为每 7 天执行，并在客户端稳定启动 2 分钟后才开始检查；运行中的环境始终跳过，关闭后会在后续周期继续处理。
- 仅清理 Chromium 可重新生成的图片、Logo、视频、代码、GPU/着色器缓存、崩溃数据和精确匹配的调试日志。
- 明确保留 Cookies、登录状态、历史记录、书签、指纹配置、IndexedDB、Local/Session Storage、Service Worker、扩展设置及钱包本地数据。
- 清理器直接访问白名单路径，不递归扫描整个环境数据目录，避免大型 `data` 目录产生长时间无效遍历。
- 全量备份和恢复复用同一份安全白名单，不再打包可再生缓存，同时继续完整保留账号、扩展与钱包数据。

## 扩展分配终审修复

- 扩展管理页面初始化改为只读，不再自动迁移、下载或分配旧版全局扩展。
- 仅在用户保存“全局使用”、点击“分配”或创建继承全局选择的新环境时处理扩展。
- 显式分配会移除 `--disable-extensions`、`--disable-extensions=...` 和 `--disable-extensions-except=...` 等阻止加载的参数，再持久化 `--load-extension`。
- 已存在相同 ID、名称或来源的扩展继续保留，不覆盖扩展目录及钱包、Cookies 等环境数据。

## 升级兼容

- 可从现有版本直接在线升级到 v1.7.43，无需先安装中间版本。
- 升级继续保留现有 `data`、环境编号、Cookies、浏览器账号、扩展与钱包数据、代理池、内核和激活状态。
