# BrowserStudio v1.7.77

## 同步标签接管（按环境进程）

- **新开环境**：第一次参与同步时，只执行一次「仅保留一个 about:blank」。
- **已在使用的环境**：再次同步或有新窗加入时，**不再关标签**，保留用户已打开的网页、扩展页与操作。
- **扩展与后台**：不关闭 service worker / background，不禁用扩展、不碰钱包数据。
- **停止后重开**：同一环境重新启动视为新进程，再接管一次后仍只执行一次。

## 含近期能力

- v1.7.76：同步启动 sole-blank 接管；启动路径轻量（prefs + session 丢弃，无 CDP 扫页）。
- v1.7.75：扩展全页滚动同步、平铺 0 间隙。

## 升级说明

- 支持从 v1.7.76 及更早版本在线升级到 v1.7.77。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展包库、代理池、浏览器内核或激活数据。
