# BrowserStudio deletion ledger

This ledger makes code removal reversible. A release cleanup is accepted only
when the removed path has no active caller, a tested replacement exists, or the
behavior is intentionally retired.

## Rules

1. Search all tracked source files for callers before deleting an implementation.
2. Preserve or add tests for the replacement behavior before removing old tests.
3. Record the last known revision, reason, replacement, verification, and restore
   command for every material deletion.
4. Restore only the smallest required function or file. Do not revert a later
   release wholesale because that can overwrite user data and unrelated fixes.
5. Large deletions without an entry in this ledger fail the release health check.

## v1.7.33 cleanup

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-001 | Global and per-profile extension-popup polling loops in `backend/extension_popup_sizer_windows.go` | `v1.7.32` (`134d520`) | Zero active production callers; they duplicated window enumeration, could fight the synchronizer, and kept background listeners alive after startup. | `backend/sync_popup_confinement_windows.go` constrains owned wallet/extension popups only while synchronization is active. | Windows cross-compile and retained popup classification/confinement tests. | Inspect with `git show 134d520:backend/extension_popup_sizer_windows.go`; restore only the required function and its direct dependencies. |
| CLEAN-002 | Off-screen browser and service-worker DevTools restoration watchers | `v1.7.32` (`134d520`) | The browser no longer launches windows off-screen. These recovery loops were legacy compensation and could unexpectedly move user windows. | Bounded `enforceBrowserWindowBounds` during startup plus sync-owned popup confinement. | Windows cross-compile, browser-window startup tests, and synchronization tests. | Inspect with `git show 134d520:backend/extension_popup_sizer_windows.go`; reintroduce behind a focused regression test only. |
| CLEAN-003 | `BOOST_BROWSER_ENABLE_GLOBAL_WINDOW_WATCHERS`, its startup branch, non-Windows stubs, and stale crash-probe comments | `v1.7.32` (`134d520`) | The option activated only the retired duplicate watchers and was disabled by default. | No replacement option is needed; active synchronization owns its listener lifecycle. | Repository symbol scan and full Go tests. | Inspect `main.go`, `backend/extension_popup_sizer_stub.go`, and `backend/app_instance.go` at `134d520`. |
| CLEAN-004 | Unused `ensureDefaultSearchProvider` and `asJSONString` compatibility helpers | `v1.7.32` (`134d520`) | Zero callers; current search-provider setup uses the maintained launch/CDP path. | `seedDefaultSearchEngine` and current core launch helpers. | Repository symbol scan and full Go tests. | Inspect with `git show 134d520:backend/extension_startup_cleanup.go`. |
| CLEAN-005 | Tests that exercised only the retired global/off-screen popup watchers | `v1.7.32` (`134d520`) | They prevented removal of dead code but did not cover the active synchronization path. | Compact tests for sync popup confinement, title classification, main-window exclusion, DevTools classification, and IME exclusion. | Windows test binary compilation and full Go tests. | Inspect with `git show 134d520:backend/extension_popup_sizer_windows_test.go`. |

## v1.7.37 sync collection cleanup

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-006 | One-second synchronization profile polling plus focus, visibility, and browser lifecycle event refresh paths in `frontend/src/modules/browser/pages/WindowSyncPage.tsx` | `v1.7.36` (`2573373`) | Concurrent automatic refreshes could apply an older PID/window snapshot after the user stopped, reconfigured, closed a window, or selected a new master. The idle panel also scanned window state continuously. | Action-driven `loadProfiles`: initial open, manual refresh, select all, choose master, and start are explicit collection boundaries. `releaseCollectedSyncData` clears the session on stop, reconfigure, and exit. | Frontend production build, full Go tests, Windows cross-compile, and `test_sync_window_collection_is_action_driven`. | Inspect with `git show 2573373:frontend/src/modules/browser/pages/WindowSyncPage.tsx`; restore only a specific event trigger after adding an ordering/cancellation regression test. Do not restore the interval as a group. |
| CLEAN-007 | Throttled asynchronous `syncRuntimeDiscovery` state and `reconcileSyncRuntimeStateAsync` goroutine in `backend/app_sync_api.go` | `v1.7.36` (`2573373`) | The async second scan returned `no_window` first and could mutate profile state after a later user action, creating stale master/follower selections. | `getSyncProfilesLocal` reconciles once synchronously only when explicitly requested; `startInputSyncLocal` performs a final authoritative reconciliation before resolving HWNDs. | Full Go tests, Windows cross-compile, code-health tests, and manual state-transition review. | Inspect with `git show 2573373:backend/app_sync_api.go`; recover the two symbols only if a future non-blocking design carries a generation token and cancellation test. |

## v1.7.38 proxy persistence and verification cleanup

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-008 | Full proxy-table delete/reinsert workflow used by every single edit and deletion in `backend/app.go` and `ProxyPoolPage.tsx` | `v1.7.37` (`38c33c5`) | One-row actions rewrote the entire pool, synchronously queried groups, and scanned all profile bindings, so response time grew with the number of proxies and environments. | `UpsertBrowserProxy`, `DeleteBrowserProxies`, transactional `ReplaceAll`/`DeleteMany`, local group derivation, optimistic deletion, and background binding reconciliation. | DAO isolation tests, frontend production build, full Go tests, and packaging tests. | Inspect `backend/app.go`, `backend/internal/browser/proxy_dao.go`, and `frontend/src/modules/browser/pages/ProxyPoolPage.tsx` with `git show 38c33c5:<path>`; restore only the compatibility `SaveBrowserProxies` fallback if a non-SQLite storage backend requires it. |
| CLEAN-009 | Sequential two-round HEAD/GET probing of every URL for each HTTP/HTTPS/SOCKS5 candidate in `backend/internal/proxy/speedtest.go` | `v1.7.37` (`38c33c5`) | A failed or mislabeled proxy could block for tens of seconds, and HTTP 407 responses were incorrectly accepted as proof of connectivity. | Concurrent protocol candidates, GET-based multi-endpoint fallback under a six-second candidate budget, three-second request slices, and explicit 407 rejection. | Proxy status regression test, full Go proxy tests, and Windows cross-compile. | Inspect with `git show 38c33c5:backend/internal/proxy/speedtest.go`; reintroduce an endpoint only behind a bounded-budget regression test, never the nested retry loops. |

## v1.7.42 action-driven extension cleanup

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-010 | Startup calls and helpers for extension-registry injection, manifest validation, stale preference cleanup and toolbar pinning in `backend/app_instance.go` and `backend/app_extension_import.go` | local pre-release `17bf730` | They performed filesystem and preference checks on every environment start, could overwrite user intent, and duplicated work already decided by the explicit distribution action. | `BrowserProfileImportExtension` and `BrowserGlobalExtensionImport` check ID/name/source only when the user executes distribution; profile launch uses the already persisted `--load-extension` argument without extension maintenance. | Full Go tests, explicit duplicate/no-overwrite tests, frontend production build and Windows cross-compile. | Inspect with `git show 17bf730:backend/app_extension_import.go`; restore only a narrowly scoped action-time check with a regression test. Never restore it to `browserInstanceStartInternal`. |
| CLEAN-011 | Automatic extension-compatible core selection in `backend/extension_core_compatibility.go` | local pre-release `17bf730` | Kernel selection is a user/profile setting and must not change implicitly during startup. The helper also inspected marker files on every launch. | No startup replacement. The selected profile core remains authoritative; compatible kernels continue to be packaged and selectable in Core Management. | Source scan confirms no startup compatibility hook; full Go tests and Windows cross-compile. | Inspect `git show 17bf730:backend/extension_core_compatibility.go`; reintroduce only as an explicit user-invoked compatibility diagnostic, not automatic switching. |
| CLEAN-012 | Removal-time deletion of Chrome extension settings, pinned entries, IndexedDB, service-worker cache and wallet extension storage | local pre-release `17bf730` | Unassigning BrowserStudio must not destroy an extension already installed by the user or its wallet/account data. | Removal now deletes only BrowserStudio's persisted assignment and `--load-extension` path; Chrome profile data remains untouched. | Regression review of remove paths plus complete Go test suite. | Inspect the cleanup functions in `git show 17bf730:backend/app_extension_import.go`; restore only for a separately confirmed destructive-delete action with explicit user confirmation. |

## v1.7.43 safe data-retention cleanup

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-013 | Destructive cache-cleaner entries for Cookies, IndexedDB, Local Storage, Session Storage, WebStorage, Storage, File System, blob storage and Service Worker storage | `v1.7.42` (`201a28a`) | These locations contain login sessions, site state and wallet-extension data; treating them as cache could cause irreversible account and wallet loss. | One shared disposable-data allowlist removes only regenerable image/media/render/code caches, Favicons/Top Sites, crash artifacts and exact browser debug logs. Backup export/import uses the same allowlist. | Preservation tests cover Cookies, wallet IndexedDB, Local/Session/Web storage, extension settings and nested extension cache names; full Go tests and frontend production build pass. | Inspect with `git show 201a28a:backend/internal/cachecleanup/cachecleanup.go`. Do not restore the removed paths; add future cache categories only with an explicit preservation regression test. |
| CLEAN-014 | Five-second one-shot startup cache-cleaner goroutine and running-profile cookie clearing | `v1.7.42` (`201a28a`) | It overlapped startup, never retried, and the running-profile branch explicitly cleared Cookies. | A cancellable scheduler waits two minutes, checks every six hours, runs weekly, skips live profiles and keeps cleanup due until skipped profiles can be processed. | Scheduler lifecycle review, cache due tests, full Go tests and frontend production build. | Inspect with `git show 201a28a:backend/app_cache_cleanup.go`; restore neither cookie clearing nor startup-overlapping timing. |
| CLEAN-015 | Extension-management page-load migration that called global extension installation for legacy localStorage entries | `v1.7.43` (`77ef150`) | Opening the page could download and distribute extensions without a current user action, contradicting the action-driven extension lifecycle. | Page load is read-only. Global installation now occurs only when the user saves an explicit global configuration or clicks “分配”. Explicit assignment also removes every Chrome extension-blocking launch flag before persisting `--load-extension`. | Source call-site audit, extension blocker regression test, full Go tests and frontend production build. | Inspect with `git show 77ef150:frontend/src/modules/browser/pages/ExtensionManagementPage.tsx`; restore only as a visible user-invoked migration command, never inside initialization. |

## v1.7.44 synchronization concurrency cleanup

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-016 | Synchronous `SendMessageTimeoutW` delivery from the low-level mouse hook for Chrome popup surfaces | `v1.7.43` (`a97721d`) | One slow popup could block the hook for 120 ms per message and serialize that delay across every follower, eventually causing lost input or Windows hook removal. | Ordered asynchronous `PostMessageW` delivery keeps hover-before-click ordering per target without waiting on a follower from the hook callback. | Windows test-binary compilation, layout-generation tests, full Go tests and frontend production build. | Inspect `git show a97721d:backend/app_input_syncer.go`; do not restore synchronous delivery inside a hook. If acknowledgement is ever required, implement it in a bounded worker queue. |
| CLEAN-017 | Full window rescans inside “全选” and “设为主控”, plus independent profile/status requests | `v1.7.43` (`a97721d`) | A normal refresh/select/master/start workflow queued several identical CIM/window scans and could combine profiles and session status from different Start/Stop boundaries. | Coalesced explicit refresh, local selection against the refreshed snapshot, authoritative validation at Start, and atomic `GetSyncSnapshot`. | Packaging action-boundary test, frontend production build, Windows compilation and full Go tests. | Inspect `git show a97721d:frontend/src/modules/browser/pages/WindowSyncPage.tsx`; restore an extra scan only for an explicit refresh action, never for every selection click. |
| CLEAN-018 | URL worker reads from the mutable `urlStopCh` field while Stop closes and clears it | `v1.7.43` (`a97721d`) | Concurrent field mutation was a data race and could turn the worker select case into a nil channel, delaying a hard stop. | Each URL worker captures its immutable session stop channel; follower CDP writes are serialized per debugging port. | Windows compilation, hard-stop lifecycle tests and full Go tests. | Inspect `git show a97721d:backend/app_input_syncer.go`; preserve immutable per-session cancellation if the worker is refactored. |
| CLEAN-019 | Duplicate Toolhelp process-snapshot parsing inside the single-window and batch-window resolvers | `v1.7.43` (`a97721d`) | Repeated local structure declarations made Chrome process ownership fixes easy to diverge and encouraged additional popup-specific scanners. | Shared `snapshotProcessChildren` and `mapProcessTreeRoots` helpers serve batch window resolution and lazily resolve wallet notification child processes only when the Win32 owner chain is missing. | Process-owner regression test, Windows compilation, Windows vet and full Go tests. | Inspect `git show a97721d:backend/win32_helpers.go`; restore no duplicate snapshot loop—extend the shared process-tree helpers instead. |
| CLEAN-020 | Unused localhost synchronization HTTP bridge and non-Windows stub in `backend/sync_bridge_windows.go` and `backend/sync_bridge_other.go` | `v1.7.43` (`a97721d`) | The bridge had zero callers, duplicated the direct synchronization API on a fixed port, and could bypass the panel's session ownership and atomic snapshot boundaries if accidentally re-enabled. | The isolated synchronization panel calls the bound App API directly and reads one shared-runtime `GetSyncSnapshot`; no listener is opened. | Repository caller scan, retired-symbol release guard, frontend build, Windows compilation and full Go tests. | Inspect with `git show a97721d:backend/sync_bridge_windows.go`; restore only if a future external client requires authenticated IPC with explicit lifecycle ownership—never restore the unauthenticated fixed localhost endpoint. |
| CLEAN-021 | Thirteen unreferenced private helpers across launch, backup, sync input, badge, wallet import, extension import, proxy test and cloak integration files | `v1.7.43` (`a97721d`) | Each symbol had only its declaration and no caller, interface contract or generated binding. Keeping obsolete alternate implementations made later fixes appear to affect active behavior and retained unnecessary imports. | Maintained call paths already implement each active behavior; no runtime replacement is needed. | Whole-repository private-function occurrence scan followed by full Go tests, Windows compilation/vet, frontend build and packaging tests. | Inspect the named files at `a97721d`; restore a single helper only together with a real caller and focused regression test. |

## v1.7.45 main-client runtime authority cleanup

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-022 | Sync-panel process discovery, runtime reconciliation and “prefer locally reconciled PID” branch | `v1.7.44` (`3245843`) | The independent panel is a separate process and cannot own or reconstruct the main client's environment state. Its scan could replace the main PID with a Chrome child/sibling PID and report every visible environment as `no_window`. | The main client publishes its running environment snapshot, including PID, debug port, HWND and window PID. Panel open, refresh and Start consume that snapshot; Stop only releases sync resources. | Main-state replacement/empty-state tests, full Go tests, Windows compile/vet, frontend build and packaging tests. | Inspect `git show 3245843:backend/browser_runtime_snapshot_windows.go` and `backend/app_sync_api.go`; never restore panel-side reconciliation. If transport changes, preserve the main-client-only runtime owner. |

## v1.7.46 popup geometry cleanup

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-023 | Wallet-title-specific fixed `390x620` popup sizing, the pause-state geometry gate, and the process-wide `--window-size=1400,600` launch argument | `v1.7.45` (`dac1d94`) | Fixed wallet dimensions ignored the current tile/stack/horizontal rectangle, while Chrome could reuse the launch argument for later secondary windows; both paths produced unused white canvas. Pausing input also disabled containment even though the user could continue opening popups. | Chrome/extensions retain their natural popup size; the synchronization geometry owner only clamps an overflowing surface into its current environment rectangle and continues doing so while input is paused. A bounded Win32 startup session applies `1400x600` only to the initial environment frame and is cancelled by explicit arrangement. | Natural-size preservation, current-owner containment, pause-state and startup-template tests, source policy guard, full Go tests, frontend production build and Windows compile/vet. | Inspect `git show dac1d94:backend/app_instance.go`, `backend/sync_popup_confinement_windows.go`, and `backend/extension_popup_sizer_windows.go`; restore neither fixed target nor the process-wide size argument. Add future popup sizing only from measured content with an arranged-owner regression test. |
| CLEAN-024 | Startup policy that closed `about:blank` while preserving every automatically opened extension onboarding/unlock page | `v1.7.45` (`dac1d94`) | With multiple assigned extensions, every environment opened several wallet tabs and displaced the requested blank initial page. | A bounded startup-only CDP cleanup closes extension page targets while preserving `about:blank`, normal web pages and extension background/service-worker targets. Extensions remain installed and can be opened normally after startup. | Startup target classification tests, full Go tests, frontend build and Windows compile/vet. | Inspect `git show dac1d94:backend/extension_startup_cleanup.go`; restore only if a future explicit “show extension onboarding at startup” user option is added. Never restore it as the default. |

## v1.7.47 extension startup event cleanup

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-025 | Two fixed startup cleanup passes at debug-ready and `+1.2s`, raw single-response WebSocket handling, and the proposed eight-second target event listener | `v1.7.46` (`8214ec8`) | Repeated passes and a resident event listener make a simple launch cleanup stateful, consume resources per environment, and can interfere with a user opening an extension immediately after startup. | Every environment start or restart performs exactly one target enumeration after its debug endpoint is ready, closes only top-level extension targets with no opener, then closes the CDP connection before returning. No polling, timer, registry, listener or extension-file modification remains. | Startup target classification, user-opener preservation, source policy guard, full Go tests, frontend build and Windows compile/vet. | Inspect `git show 8214ec8:backend/extension_startup_cleanup.go`; restore neither fixed-delay retries nor a resident listener. If a kernel exposes its targets later, move the single sweep to that kernel's definitive ready boundary instead of adding background monitoring. |

## v1.7.48 single-owner startup presentation

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-026 | Explicit command-line `about:blank`, startup extension-page exemption based on `openerId`, and the four-attempt `startupWindowBoundsSession` worker/cancel registry | `v1.7.47` (`7490461`) | Preferences plus a command-line URL created duplicate blank tabs/windows; extension background pages commonly set an opener and therefore escaped cleanup; one retry worker per environment added unnecessary startup concurrency. | Preferences exclusively create one blank page. The initial frame starts minimized, one startup sweep closes every top-level extension page, and one synchronous window restore/size operation presents the completed environment. | Launch-argument ownership test, opener-bearing extension target regression, full Go tests, frontend production build, Windows compile/vet, packaging and code-health tests. | Inspect `git show 7490461:backend/app_instance.go`, `backend/extension_startup_cleanup.go`, and `backend/extension_popup_sizer_windows.go`; restore none wholesale. If a kernel fails to expose its main HWND at debug-ready, add a kernel-specific ready signal before the single restore call, not a retry worker. |
| CLEAN-027 | Per-environment `lastTabsTracker`, its two-second CDP target polling loop, stop-time tab capture and tracker registry | `v1.7.47` (`7490461`) | The authoritative launch policy no longer restores previous tabs, yet every running environment still enumerated all CDP targets every two seconds and periodically wrote the database. With many environments this created permanent CPU, CDP and persistence load unrelated to the requested blank startup. | Launches continue to use only explicit start URLs. Existing `LastTabs` fields and stored values remain untouched for old-data/schema compatibility; no runtime collector overwrites them. | Blank/default and explicit-URL launch tests, retired-symbol packaging guard, full Go tests, frontend build and Windows compile/vet. | Inspect `git show 7490461:backend/app_last_tabs.go`; restore only a user-enabled session-restore feature with one lifecycle owner and a non-polling browser event source. Do not delete or migrate existing `LastTabs` data. |
| CLEAN-028 | No-op `resolveCloakGeoArgs`, unused country/language header helpers, their misleading startup override branch and helper-only tests | `v1.7.47` (`7490461`) | The runtime resolver returned `nil` for every proxy and never performed the documented lookup, while the remaining helpers had no production caller. The dead branch implied fingerprint behavior that did not exist and complicated launch argument ownership. | User/profile fingerprint arguments remain authoritative; active proxy routing, Cloak local-state flags and selected-core launch behavior are unchanged. | Whole-repository caller scan, retired-symbol packaging guard, full Go tests and Windows compile/vet. | Inspect `git show 7490461:backend/cloak_integration_helpers.go` and `backend/cloak_geoip_test.go`; recover only as an explicit, privacy-reviewed user action with bounded networking and focused runtime tests. |
| CLEAN-029 | Uncalled window-bounds tracker registry, two-second CDP polling worker, stop-time CDP fetch and asynchronous persistence path in `backend/app_window_bounds.go` | `v1.7.47` (`7490461`) | Every tracker start call had already been disabled, but the complete alternate lifecycle and a no-op stop call remained. Keeping it invited accidental reactivation beside the startup template and synchronization geometry owner. | The initial environment frame is sized once after startup cleanup; later tile/stack/horizontal layout is owned only by the explicit synchronization action. Existing `LastWindow*` fields and stored values remain untouched for data compatibility. | Whole-repository caller scan, retired-symbol packaging guard, full Go tests, frontend build and Windows compile/vet. | Inspect `git show 7490461:backend/app_window_bounds.go`; recover only a user-invoked save/restore feature with one lifecycle owner and focused tests. Do not delete or migrate stored `LastWindow*` values. |

## v1.7.49 single blank-page authority

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-030 | Preferences ownership of `session.startup_urls=[about:blank]` and `restore_on_startup=4`; positional startup URLs retained in raw profile/API launch arguments | `v1.7.48` (`47dca91`) | Chrome creates its initial page target before applying configured startup URLs, so the preference-generated blank could coexist with the initial blank. Old raw launch arguments could also reintroduce blank, web or extension startup tabs. Post-start deduplication would hide rather than remove those competing owners. | Preferences use the normal startup mode without `startup_urls`; `preparePrimaryEnvironmentLaunchArgs` removes positional startup targets and owns exactly one command-line `about:blank`. Explicit product start URLs remain owned by the later CDP navigation path. No post-start blank-page scan or merge exists. | Preference ownership, stale-target removal and exactly-once launch-argument tests, packaging lifecycle guard, full Go tests, frontend build and Windows compile/vet. | Inspect `git show 47dca91:backend/extension_startup_cleanup.go` and `backend/browser_launch_args.go`; restore configured or raw startup URLs only if the command-line URL owner is removed in the same change. Never keep multiple owners or add post-start blank deduplication. |
| CLEAN-031 | Immediate extension-page target enumeration at debug-port readiness | `v1.7.48` (`47dca91`) | The debug endpoint can become ready before assigned wallet extensions finish loading, so the single early enumeration released successfully but missed onboarding tabs created moments later. | One synchronous startup barrier observes only the explicitly assigned extension IDs, allows one 500 ms completion window with a hard 2-second deadline, reads the final target list, closes top-level extension pages once, then releases all HTTP/CDP connections. It creates no goroutine, listener, timer, signature tracker, registry or post-start worker and leaves extension background/service-worker targets running. | Assigned-ID extraction, ready/completion-window and page/background classification tests, source lifecycle guard, full Go tests, frontend build and Windows compile/vet. | Inspect `git show 47dca91:backend/extension_startup_cleanup.go`; restore the immediate enumeration only if the selected browser core later provides a definitive extension-ready signal before debug readiness. Never combine the early pass with this bounded barrier. |

## v1.7.50 browser-owned initial page

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-032 | Command-line `about:blank` ownership and completion based only on extension IDs parsed from `--load-extension` | `v1.7.49` (`8eee81e`) | The browser core already creates its own initial target, so a positional blank created duplicate tabs. Extensions persisted in an existing profile need not appear in the current launch argument, allowing wallet onboarding tabs to open after the old ID-based barrier had completed. | BrowserStudio creates no default page. A synchronous startup-only barrier preserves the first natural Chrome blank, immediately closes additional blank and top-level extension page targets observed from the actual browser, leaves extension workers/background targets intact, then closes its single CDP connection and releases all state. | No-default-target launch test, persisted-extension/late-target cleanup regression, packaging lifecycle guard, full Go tests, frontend production build and Windows compile/vet. | Inspect `git show 8eee81e:backend/browser_launch_args.go` and `backend/extension_startup_cleanup.go`; recover no positional default URL. If a future core provides a definitive all-extensions-ready event, it may replace the bounded startup barrier, but must preserve the browser-owned single initial page and startup-only lifecycle. |

## v1.7.51 passive extension distribution

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-033 | Automatic global-extension inheritance during single/batch profile creation and save-time global installation in `backend/app.go`, `backend/app_extension_import.go`, and `frontend/src/modules/browser/pages/ExtensionManagementPage.tsx` | `v1.7.50` (`e45ced5`) | Creating an environment or merely saving an extension configuration could mutate launch arguments, preferences and extension files without a separate distribution action. It also made a global choice behave like a recurring policy rather than a completed user command. | Only the visible “分配” action calls the import APIs. Global distribution records the current profile IDs, skips completed/current environments without rescanning or overwriting them, and checks only newly created profile IDs when the user explicitly clicks “分配” again. Browser startup and profile creation never read the global registry. | Explicit-action source guards, no-inheritance regression test, global completion-set tests, full Go tests, frontend production build and Windows compile/vet. | Inspect `git show e45ced5:backend/app_extension_import.go`, `git show e45ced5:backend/app.go`, and `git show e45ced5:frontend/src/modules/browser/pages/ExtensionManagementPage.tsx`; restore only a visible user-invoked distribution action. Never restore create-time, save-time, startup or background synchronization. |
| CLEAN-034 | Configuration-only environment deletion and its optional “delete cache/data” checkbox in `backend/internal/browser/profile.go`, `backend/app.go`, and `frontend/src/modules/browser/pages/BrowserListPage.tsx` | `v1.7.50` (`e45ced5`) | Repeated delete/create operations left unowned Chrome profiles containing caches, Cookies and extension/wallet storage under `data`, causing silent disk growth and ambiguous recovery candidates. | Every UI, legacy Wails and Launch API delete path now requires the environment to be stopped and removes its owned user-data directory. The App deletion owner also removes profile snapshots and stale manual/global extension assignment references without deleting shared extension packages used by other environments. | Permanent-delete UI/source guards, owned-data and registry cleanup tests, full Go tests, frontend production build and Windows compile/vet. | Inspect `git show e45ced5:backend/internal/browser/profile.go` and `git show e45ced5:frontend/src/modules/browser/pages/BrowserListPage.tsx`; restore configuration-only deletion only as a separately named archival workflow that moves data to a recoverable archive and never leaves an anonymous directory. |
| CLEAN-035 | In-flight sync-profile request coalescing through `loadProfilesPromiseRef` in `frontend/src/modules/browser/pages/WindowSyncPage.tsx` | `v1.7.50` (`e45ced5`) | A user refresh issued while the panel's opening request was still running reused that older promise, so a newly started environment could remain absent even though the user explicitly requested a fresh scan. | Every panel open or Refresh action now starts one explicit snapshot request. A monotonic request sequence prevents an older result from overwriting the latest result; no timer or continuous scanner was added. | Frontend production build, source guard, full Go tests and Windows compile/vet. | Inspect `git show e45ced5:frontend/src/modules/browser/pages/WindowSyncPage.tsx`; only restore request coalescing for non-refresh reads, never for the explicit Refresh action. |

## v1.7.52 user-owned extension activation

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-036 | Two-second/50 ms startup target polling loop, its fixed settle delay/protected-URL and opener-ownership compatibility branches in `backend/extension_startup_cleanup.go`, plus delayed-debug re-entry in `backend/browser_runtime_state.go` | `v1.7.51` (`f575991`) | Polling and delay could overlap with normal wallet use; URL/opener exceptions duplicated lifecycle ownership and allowed automatic startup pages to escape; delayed-debug cleanup could run after the browser had already been handed to the user. | Every new browser process executes one immediate target snapshot after stable debug readiness while its window is still minimized, before stealth setup, explicit URL navigation or user handoff. At that boundary every top-level extension page is necessarily startup-owned, so it and extra natural blanks are closed while the first blank and extension workers remain. CDP is then closed and all later user/web/app extension activity stays outside the cleanup lifecycle. Delayed-debug attachment never cleans tabs. No DOM, form, secret or page content is read. | One-snapshot release and opener-bearing startup-page tests, source-order/lifecycle/privacy guards, full Go tests, frontend production build and Windows compile/vet. | Inspect `git show f575991:backend/extension_startup_cleanup.go` and `backend/browser_runtime_state.go`; recover only a narrowly scoped target classifier if required. Never restore polling, a settle delay, URL/opener exceptions, or cleanup after user handoff. |

## Wallet/profile storage integrity hardening

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-037 | Ordinary profile update assignment `profile.UserDataDir = input.UserDataDir` and editable existing-environment data-directory field | `v1.7.52` (`89786aa`) | A blank or changed edit/API payload silently remapped an existing environment away from its Cookie, extension and encrypted wallet directory, making intact data appear lost and leaving the old folder orphaned. | Profile ID permanently owns one canonical user-data directory. Create and launch reject cross-profile collisions; ordinary updates preserve that identity; only the existing controlled legacy-data recovery workflow may replace data. | Storage-remap rejection, blank-update preservation, absolute/relative collision and 200-environment uniqueness regressions; full verification matrix recorded with the release. | Inspect `git show 89786aa:backend/internal/browser/profile.go` and `frontend/src/modules/browser/pages/BrowserEditPage.tsx`. Do not restore direct assignment; add a separately named transactional migration API if user-directed relocation is required. |
| CLEAN-038 | Profile Create/Update path that mutated the in-memory map first and called `SaveProfiles`, rewriting every environment row | `v1.7.52` (`89786aa`) | One environment edit performed O(N) database writes and could leave memory changed after a persistence failure. With 200 environments and frequent edits this amplified lock time and partial-state risk. | `commitProfileLocked` writes only the affected SQLite row before publishing the new immutable profile value; the legacy file backend stages and rolls back its map entry on failure. | Focused CRUD/storage tests, full Go tests, Windows build/vet, frontend production build and packaging checks. | Inspect `git show 89786aa:backend/internal/browser/profile.go`; recover the full-catalog write only inside the legacy config-file backend, never for SQLite CRUD. |
| CLEAN-039 | Snapshot restore sequence that deleted the live user-data directory before validating and extracting the ZIP | `v1.7.52` (`89786aa`) | A corrupt ZIP, disk-full condition or extraction error could irreversibly remove Cookies and encrypted wallet state before recovery was known to be viable. | Restore extracts into a sibling staging directory, renames the current data aside, atomically installs the validated staging tree and rolls back the original directory if the switch fails. | Snapshot and ZIP-focused tests plus full verification matrix recorded with the release. | Inspect `git show 89786aa:backend/app_snapshot.go`; never restore delete-before-extract. If cross-volume restore is added, retain a complete rollback copy until post-copy verification succeeds. |
| CLEAN-040 | Profile deletion sequence that permanently removed browser data before deleting the profile database record | `v1.7.52` (`89786aa`) | A database/config persistence failure after filesystem deletion left a live environment record pointing to irreversibly deleted wallet data. | Deletion first renames the owned directory to an isolated sibling, persists removal of the environment record, then removes the isolated tree; metadata failure renames the complete data back. | Owned-data deletion regression, full Go tests and Windows build/vet. | Inspect `git show 89786aa:backend/internal/browser/profile.go`; never restore delete-before-metadata. If a recycle-bin workflow is added, use the same staged ownership boundary. |
| CLEAN-041 | Four independent direct/temp-file JSON writers for Chromium Preferences, Local State and extension assignment registries | `v1.7.52` (`89786aa`) | Direct `os.WriteFile` could expose a truncated Preferences file after interruption, while hand-written rename paths did not consistently replace existing files on Windows. Chrome can then reset extension registration, making intact wallet storage appear lost. | One `writeFileAtomically` owner writes, flushes and closes a same-directory temporary file; Unix renames over the destination and Windows uses `MoveFileEx(REPLACE_EXISTING|WRITE_THROUGH)`. | Atomic replacement regression, startup/extension preference tests, Windows compile/vet and full Go tests. | Inspect the affected writers with `git show 89786aa:backend/extension_startup_cleanup.go`, `backend/app_extension_import.go`, `backend/cloak_flags_seed.go`, and `backend/cloak_integration_helpers.go`; restore no direct Preferences overwrite. |

## v1.7.53 recoverable data identity and clean-close lifecycle

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-042 | Permanent removal of an environment user-data directory and its snapshots after profile deletion | local pre-release after `89786aa` | The clarified product rule requires deleted, renamed or replaced environments to remain recoverable without silently destroying Cookies, extension storage or encrypted wallet state. | Deletion atomically moves the complete owned directory into `.browserstudio-recovery/deleted-environments`, writes a non-secret identity sidecar and retains snapshots. Startup only reports available archives; restoration remains an explicit user-confirmed action. | Archive/rollback/Cookie-preservation tests, startup recovery-count tests, legacy-data preview tests and the full release verification matrix. | Recover the old permanent-removal block only from the pre-release diff if a separately named irreversible purge action is later approved with explicit confirmation. Never restore it to ordinary environment deletion. |
| CLEAN-043 | Direct `Process.Kill`, forced `taskkill /F`, manual runtime-state clearing and parallel shutdown close ownership in `SyncCloseAll`, client shutdown and backup maintenance | local pre-release after `89786aa` | Killing Chromium before its LevelDB/SQLite close handlers finish can lose the latest wallet, Cookie or extension state and can mark an environment stopped while its data lock is still active. Multiple close owners also raced over Profile ID/PID state. | Every user-visible environment close is routed through `BrowserInstanceStop`: it atomically records Profile ID/directory/PID, requests `Browser.close` (or a non-forced OS close), waits for the process, debug endpoint and Singleton lock to disappear, writes a clean-close data pointer, then marks the exact environment stopped. Failure never escalates to a forced kill or falsely reports successful persistence. | Data-pointer integrity/recovery tests, close-barrier tests, source guards prohibiting forced close in sync/shutdown, full Go tests and Windows compile/vet. | Inspect the removed functions in the pre-release `backend/app_shutdown.go` and `backend/app_sync_api.go`; recover no forced browser termination. A future emergency-kill command must be separately named, explicitly confirmed and must warn that current browser data cannot be certified saved. |
| CLEAN-044 | Backend-local atomic JSON writer aliases and their parallel platform implementations | local pre-release after `89786aa` | Keeping the same file-replacement lifecycle in the backend package and `internal/fsutil` created two owners that could diverge on Windows durability and error handling. | `internal/fsutil.WriteFileAtomic` is the single owner for Preferences, Local State, extension registries, recovery manifests and environment data pointers. | Atomic replacement test on the host, Windows cross-build/vet and caller scan confirming no second mutable-file replacement implementation. | Inspect the removed backend-local files in the pre-release diff; restore only a missing platform primitive inside `internal/fsutil`, never another package-level wrapper. |

## v1.7.53 environment-owned popup layering

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-045 | Wallet-title-only process-tree fallback and geometry-only `SWP_NOZORDER` placement in `backend/sync_popup_confinement_windows.go` | local pre-release after `89786aa` | Other extension/permission windows could miss their environment owner, while geometry-only placement preserved a popup's desktop-wide topmost state and let it cover unrelated environments. | The existing sync-session confinement owner resolves every candidate through the Chrome process tree, demotes `WS_EX_TOPMOST`, groups all surfaces by root environment and preserves their native top-to-bottom order as one normal-Z block directly above that environment. Natural content and native vertical/horizontal overflow remain untouched. | Focused ownership/candidate/placement flag tests, source lifecycle guard, Windows compile/vet and full release verification matrix. | Inspect the pre-release version of `backend/sync_popup_confinement_windows.go`; recover no wallet-only gate or unconditional `SWP_NOZORDER`. If a core needs special ownership metadata, add it inside the same session-owned resolver with a regression test. |

## v1.7.53 triple-audit data close and rollback hardening

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-046 | Windows fallback `taskkill /PID /T` in the ordinary environment-close lifecycle | local pre-release after `89786aa` | `taskkill` does not provide the same provable window-close and Chromium database flush contract as `Browser.close`/`WM_CLOSE`; retaining it could terminate extension storage before the close barrier completed. | CDP remains first choice; the Windows fallback posts `WM_CLOSE` to top-level Chromium windows in the exact environment process tree. The existing process/debug/Singleton-lock barrier must still pass before stopped state or clean-close metadata is published. | Source guard banning taskkill, Windows compile/vet, clean-close tests and full Go regression. | Inspect the pre-release `requestSoftProcessStopPID` in `backend/app_instance.go`; do not restore taskkill. Add a separately named, explicitly confirmed emergency termination command if ever required. |
| CLEAN-047 | Silent archive/snapshot/legacy-data rollback calls that discarded filesystem restoration errors | local pre-release after `89786aa` | A failed rename could leave complete data safely preserved in the recovery archive while the caller incorrectly reported that the original directory had been restored, making the environment appear to have lost wallet or Cookie data. | All rollback functions return joined errors, refuse to overwrite an occupied destination, report the precise retained archive path, and callers distinguish successful restoration from retained-but-not-relinked data. Detached-browser direct close now also writes the same clean-close pointer after debug and profile locks disappear. | Archive rollback and legacy overwrite tests, source guards, full Go tests and Windows compile/vet. | Inspect the pre-release `ProfileDataArchiveMove.Rollback`, snapshot restore and `legacyRestoreCandidateData`; never restore ignored rollback errors or unconditional rename-overwrite behavior. |
| CLEAN-048 | Extension package replacement that deleted the preceding rollback package before the new package was committed, ignored restore errors, and profile launch-argument mutations that remained in memory after persistence failure | local pre-release after `89786aa` | An interrupted extension replacement could strand the only known-good program package, while a failed profile transaction could make the current UI state disagree with the database and change which extension appeared after restart. | Package replacement temporarily displaces the older rollback, commits the staged package by same-volume rename, restores both levels on failure, and retains the immediately previous package after success. Profile extension bind/unbind restores launch arguments and timestamps on persistence failure; running Chrome Preferences are never rewritten concurrently. | Extension replacement/rollback, failed-persistence memory rollback, running-Preferences preservation and explicit-distribution regressions, followed by the full release matrix. | Inspect the pre-release `installUnpackedExtension`, `bindExtensionDirToProfiles`, `removeExtensionDirFromProfilesExcept`, and developer-mode writer. Restore no delete-before-commit or ignored rename path. |
| CLEAN-049 | Recovery rollback ordering that deleted an environment archive sidecar before proving its data could return to the active path, plus reset-import directory removal errors that were ignored before copy continued | local pre-release after `89786aa` | If the active path was unexpectedly occupied or removal failed, complete wallet/Cookie data could remain on disk but lose its recovery mapping, or a reset import could merge into a directory it had failed to clear. | The archive mapping remains until the data rename succeeds; failed rollback reports the retained archive and leaves it discoverable. Reset import stops that component when cleanup fails instead of copying over ambiguous old contents. Snapshot reads use immutable profile copies and deletion reports filesystem failures. | Occupied-destination recovery-index regression, backup/snapshot focused tests and the full release verification matrix. | Inspect the pre-release `ProfileDataArchiveMove.Rollback`, `backupImportFileTrees`, and snapshot helpers. Never delete the recovery index before the archived directory is relinked. |
| CLEAN-050 | Startup/default-proxy reconciliation paths that mutated live profile objects before persistence and ignored or merely logged the database failure | local pre-release after `89786aa` | A transient write failure let the current process launch with generated fingerprint/proxy identity that was absent from disk, so the next restart could appear to change environment identity or lose its proxy binding. | Startup snapshots the profile, persists generated defaults, and aborts with the original profile restored on failure. Startup proxy reconciliation similarly restores every touched profile if its coordinated write fails. Status/snapshot APIs return immutable copies instead of exposing internal pointers after releasing the manager lock. | Failed-default persistence regression, proxy/profile focused tests and the full release verification matrix. | Inspect the pre-release default application block in `backend/app_instance.go` and `reconcileProfileProxyBindings`; never restore ignored `SaveProfiles` results or publish mutable manager pointers. |

## v1.7.54 delayed extension startup cleanup

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-051 | Single-snapshot `closeUnwantedStartupPagesOnce` lifecycle and its test assumption that no extension page may appear after the first debug-ready snapshot | `v1.7.53` (`2ca5ba3`) | MetaMask and other wallets can create onboarding tabs after the debug endpoint first becomes ready, especially during concurrent multi-environment startup. The one-shot snapshot therefore released ownership before those automatic tabs existed. | One `closeUnwantedStartupPagesDuringLaunch` owner observes only a 2.5-second bounded startup transaction while the window remains minimized, closes delayed extension pages and extra natural blanks, then releases. The mutually exclusive normal-ready and delayed-ready paths call the same finalizer before restoring the environment window; no listener survives handoff. | Delayed-target, hard-pass-limit and startup-order regressions, focused Go tests, packaging policy tests and the v1.7.54 verification matrix. | Inspect `git show 2ca5ba3:backend/extension_startup_cleanup.go` and restore only the removed one-snapshot helper if a future browser core provides a proven extension-initialization completion signal; do not restore the obsolete assumption or add a post-handoff listener. |

## multi-open environment popup ownership

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-052 | `InputSyncer.syncPopupBoundsLoop` / `constrainSyncPopupSurfaces` as the sole popup geometry owner | local pre-release after multi-open confiner work | Wallet, extension and secondary Chrome windows must stay inside each environment whenever multiple environments are open, even if the user never opens the sync assistant. Keeping confinement only inside an active InputSync session left free-floating TOPMOST popups covering other environments. | `environmentPopupConfiner` on the main client is the single owner. It starts when any environment is running, resolves owners for every live profile HWND, reuses the shared confine/placement helpers, keeps natural popup size with uniform proportional shrink, demotes TOPMOST and groups Z-order per environment. InputSyncer no longer starts a second loop; layout hold still yields during tile/stack. | Proportional-shrink and process-linked candidate tests, Windows cross-compile, confiner start/stop from environment lifecycle. | Inspect the removed InputSyncer methods in the pre-release `backend/sync_popup_confinement_windows.go` / `app_input_syncer.go`. Restore no second concurrent confiner. If a future design needs a different owner, keep a single lifecycle and preserve natural-size proportional shrink. |

## repository hygiene: accidental UUID staging + scaffold UI

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-053 | 123 tracked paths `backend/<uuid>/.boost-browser-import-cookies.json` plus 12 empty UUID directories under `backend/` | `codex/stable-v1.7.16-rebuild` before this cleanup (files present in working tree / git index) | Not real environment data. Each folder is a Profile UUID with only a cookie **import staging** marker written into the Go package CWD during local tests/dev. Contents are dummy fixtures (`session`/`sid` values of length 1–3 for claude.ai / x.com / google). They pollute the repository, are not read by production code, and must never be mistaken for live user-data. | Real cookie/profile storage remains under the app data root (`data/` / configured `user_data_root`). Pattern ignored via `.gitignore` `backend/*-*-*-*-*/`. No runtime code path was removed. | Inventory script confirmed value lengths ≤3 and no JWT-like blobs; `git ls-files 'backend/*-*-*-*-*'` empty after removal; package still builds. | Restore a single file with `git show HEAD:backend/<uuid>/.boost-browser-import-cookies.json` from the last commit that still contained it (before this cleanup is committed). Do **not** reintroduce the whole tree into the package directory; if a fixture is needed for tests, place it under `backend/testdata/`. |
| CLEAN-054 | `frontend/src/modules/charts/**` (ChartsPage + recharts demo components) | same branch prior to cleanup | Scaffold demo only: title「图表案例展示」with hardcoded Bar/Line/Pie/Area/Composed examples. Not part of BrowserStudio product flows; `/charts` already redirected to environment list; no navigation entry. | No product chart feature. Keep recharts dependency only if a future real dashboard needs it (evaluate separately). | Repo search shows no remaining imports of `ChartsPage` / `modules/charts`. | `git show <rev>:frontend/src/modules/charts/ChartsPage.tsx` and restore the directory if a demo gallery is required again. |
| CLEAN-055 | `frontend/src/modules/profile/AdminKeygenPage.tsx` | same branch prior to cleanup | Empty stub `export function AdminKeygenPage() { return null }`. Historical leftover from an admin CD-key generator UI; no route, no export from `profile/index.ts`, no menu entry. Backend still has `GenerateCDKeys` / `RedeemCDKey` Wails bindings used by dashboard API helpers, not by this page. | Activation remains installer offline provider + optional CD-key redeem APIs. No admin keygen UI in the shipping client (intentional for security). | File removed; search finds no `AdminKeygen` references. | Restore with `git show <rev>:frontend/src/modules/profile/AdminKeygenPage.tsx`. Rebuild a real admin tool only behind explicit auth and never ship key-generation in the public Manager build without a separate threat model. |

## v1.7.58 startup critical-path cleanup

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-056 | Blocking multi-second `closeUnwantedStartupPagesDuringLaunch` observation (1.8–3.5 s quiet-pass loop) on the environment start critical path inside `finalizeBrowserStartupTabs` | `v1.7.57` (`555186f`) | The loop held every environment start until wallet onboarding quieted, multiplying into multi-open latency. Users regressed from the fast v1.7.48 single-snapshot model. | `finalizeBrowserStartupTabs` restores one-shot `closeUnwantedStartupPagesOnce` + blank ensure on the start path; delayed wallet pages are handled only by a short background recheck (`runStartupTabCleanupDelayed`) that does not block start return. | `TestCloseUnwantedStartupPagesOnceIsSinglePass`, delayed-launch helper tests, focused Go startup-cleanup suite. | Inspect `git show 555186f:backend/extension_startup_cleanup.go`. Do not reintroduce a multi-second wait on `browserInstanceStartInternal` before handoff; keep any delayed recheck off the critical path. |

## v1.7.59 startup tab policy and extension prep

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-057 | Multi-pass / budgeted `closeUnwantedStartupPagesDuringLaunch`, 1–20 slot tab-manage budgets, and background delayed extension-tab polling after start | `v1.7.58` (`88c5e78` / post-tag packaging commits) | Long observation windows and real-time-style rechecks closed tabs the user opened (extension toolbar clicks) and kept CDP busy during multi-open. | Single-shot `finalizeBrowserStartupTabs` / `closeUnwantedStartupPagesOnce` at debug-ready only; no post-handoff watcher. Extension package prep is one-shot via `.boost_extension_launch_ready` without overwriting vaults. | Single-pass cleanup tests, extension launch-ready tests, Windows cross-compile. | Inspect `git show 88c5e78:backend/extension_startup_cleanup.go`. Do not restore multi-second post-start polling. |

## v1.7.60 startup blank policy and prep skip

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-058 | Blank-page create/keep/close logic (`ensureSingleNaturalBlank`, `trimExtraNaturalBlank`, `startupPageCloseExtraBlank`, multi-blank planning) and long post-start tab observation budgets | `v1.7.59` (`1b2366e`) | Mutually exclusive blank create-then-close logic fought Chromium's natural single startup blank and risked closing user tabs when combined with delayed polling. | Browser owns the single default blank; startup only closes automatic extension pages once at debug-ready. Hot path skips heavy prep after one-time markers. | Extension startup single-pass tests; launch-ready tests; Windows cross-compile. | Inspect `git show 1b2366e:backend/extension_startup_cleanup.go`. Do not restore blank create/close pairs. |

## v1.7.62 tab management cycle simplification

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-059 | Post-handoff multi-pass tab schedules, blank create/close/navigate patch stacks, and repeated user-triggered normalize hooks that re-managed tabs after the user owned the window | `v1.7.61` (`ad2fd1d`) | Layered after-the-fact tab fixes fought Chromium's startup prefs, could close user-opened extension tabs, and re-ran management on sync/list clicks after handoff. | Foundation: Preferences `restore_on_startup=4` + `startup_urls=["about:blank"]`. Phase-1 at every environment start: one extension-auto-page close + re-arm handoff. Phase-2 once per start cycle on user click (sync tool / main list / sync panel): collapse to sole `about:blank`, then no further tab management until next start/stop. | Preferences about:blank tests; extension auto-page close tests; Windows cross-compile. | Inspect `git show ad2fd1d:backend/extension_startup_cleanup.go`. Do not restore long post-start schedules or blank create/close pairs. |

## v1.7.63 last-tab-safe handoff and wallet input sync

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-060 | Collapse path that `Target.closeTarget` non-blank pages before a blank survivor exists (close-then-create / close last content tab), dual-process panel handoff re-collapse, and silent CDP key/mouse drops on follower match failure | `v1.7.62` (`f8f4a96` / `6fc5006`) | Closing Chromium's last page destroys the whole environment window under multi-open + sync handoff; panel process raced main handoff; wallet password/digit keys vanished when popup targets mismatched. | `planCollapseToSoleAboutBlank` navigates keep tab to about:blank first then closes siblings only; panelMode skips handoff; startup extension close ensures blank first; focused-target insertText + editable mirror + close-chord block for followers. | `TestPlanCollapseToSoleAboutBlankNeverClosesLastTabFirst`, `TestCloseAutomaticExtensionStartupPagesNeverKillsLastTab`, `TestSyncPanelNeverRunsTabHandoffCollapse`, wallet match/key unit tests; Windows cross-compile. | Inspect pre-1.7.63 `collapseEnvironmentTabsToSoleAboutBlank` in `git show f8f4a96:backend/extension_startup_cleanup.go`. Do not reintroduce close-last-tab-before-blank. |

## v1.7.64 gapless tile and Esc page-safe pause

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-061 | 1px-only internal tile overlap without frame-sized seam hide / incomplete last-row stretch; Esc resume path that compared post-pause master URL to pre-pause `lastSyncURL` and `Page.navigate`d all followers | `v1.7.63` (`8d9d741`) | Small multi-open grids left visible Chrome/DWM seams; Esc pause/resume reloaded or rewrote follower page URLs and form content. | `computeGaplessTileRects` with system frame overlap + outer bleed + last-row stretch; Esc resume `urlSyncReseed` baselines master URL/editable without navigate or value push. | `TestComputeGaplessTileRects*`, `TestEscapeResumeReseedsURLBaselineWithoutStaleNavigate`; Windows cross-compile. | Inspect `git show 8d9d741:backend/app_sync_api.go` tile loop and pre-reseed `urlSyncLoop`. Do not restore resume-time forced navigation. |

## v1.7.65 multi-env sync ownership, uniform tile, handoff isolation

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-062 | Incomplete last-row tile stretch; dual popup geometry writers (sync align-loop / multi-owner `SetWindowPos` resize thrash); global tab-handoff that re-collapsed every running profile when any batch started; SOCKS5 first-wins race treating first successful probe as scheme; extension overflow three-dot delete without confirm | `v1.7.64` (`e6ba118` / `d46b8b0`) | Last-row stretch broke click coordinate maps; wallet popups flickered under dual resize owners; opening new envs closed work tabs on other profiles; proxy scheme mis-detect; accidental extension removal. | Uniform `computeGaplessTileRects` cells; sole geometry owner `environmentPopupConfiner` + position-only sync + layout-hold flag; per-profile handoff; declared-scheme SOCKS5 prefer; labeled 移除 + ConfirmModal. | Tile uniform-size tests; layout-hold / popup position-only tests; handoff isolation; proxy scheme tests; Windows cross-compile. | Inspect `git show e6ba118:backend/tile_layout.go` last-row stretch and pre-`d46b8b0` dual SetWindowPos paths. Do not restore multi-owner popup resize or global handoff collapse. |

## v1.7.66 post-start extension tab cleanup retired (root: selective inject)

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-063 | Post-start CDP tab machinery: `finalizeBrowserStartupTabs`, `runStartupTabCleanupOnce`, `closeAutomaticExtensionStartupPagesOnce`, `collapseEnvironmentTabsToSoleAboutBlank` / `planCollapseToSoleAboutBlank`, `pendingTabHandoffProfiles` arm/take/clear, frontend `requestUserTabHandoff` / list pointer-down handoff, OpenWindowSyncPanel handoff call | `fb6d107` / pre-cleanup HEAD | Band-aid for extension auto-pages caused by re-injecting `--load-extension` every start; wiped sessions, wasted CPU, stacked with root fix. | Selective `--load-extension` + hot-settled start (`extension_launch_ready.go` / `app_instance.go`); Preferences `about:blank` foundation only; `FinalizeEnvironmentTabsForUserHandoff` no-op for API. | Prefs unit tests; selective inject tests; close-behavior retired-handoff test; Windows cross-compile. | `git show fb6d107:backend/extension_startup_cleanup.go`. Do not restore CDP close sweeps after launch; fix inject policy instead. |

## v1.7.67 Scheme A profile extensions + startup/sync safety

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-064 | Daily `--load-extension` CLI reinject as primary activation (`applySelectiveLoadExtensionArgs` inject-first path); immediate wallet force-fit resize + pure Z-order `SetWindowPos` thrash on confiner ticks; sync-start/same-URL unconditional `Page.navigate` mass reload; hot-start skip of session foundation sanitize | `v1.7.66` (`d87dcf7` / `71ddaed`) | CLI reinject reopened chrome-extension unlock/home tabs every start (double with restored session tabs); force-fit mid-paint blanked Rabby Notification under multi-open; navigate-on-sync-start dropped dapp wallet connections | Scheme A: `installUnpackedExtensionIntoProfile` + strip CLI when installed (`extension_profile_install.go`); notification hosts never resized + 2s grace + no pure Z-order; URL reseed on sync start + `urlsMatchForSync` skip; always pin about:blank + `discardChromeRestorableTabSessions`; unbind disables prefs state (keeps LES) | Profile install/disable/re-enable tests; selective→native launch tests; popup force-fit grace tests; URL match tests; session discard tests; Windows pack | `git show d87dcf7:backend/extension_launch_ready.go` selective inject; `git show d87dcf7:backend/sync_popup_confinement_windows.go` always-resize path. Do not restore daily CLI reinject or sync-start mass navigate. |

## v1.7.68 Scheme A reliability: no prefs-only CLI strip, no package copy

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-065 | Per-start multi-MB package copy into `Default/Extensions/<id>/<version>`; strip `--load-extension` based only on hand-written Preferences rows (empty permissions / INTERNAL location=1); re-install prefs rewrite every start when "already installed" | `v1.7.67` (`cc7aa2c`) | Chromium ignored incomplete INTERNAL prefs → zero toolbar extensions; package copy blocked window open ~20s+ | Unpacked prefs registration (location=4) pointing at shared package path with manifest permissions; CLI inject until durable LES/runtime files exist; no profile package copy on critical path | `TestApplyProfileNativeKeepsCLIUntilChromeDataExists`, install-no-copy tests, selective inject tests; Windows pack | `git show cc7aa2c:backend/extension_profile_install.go`. Do not strip CLI on Preferences-only rows or copy full CRX trees per environment start. |

## v1.7.69 Scheme A integrity freeze + sync prompts

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-066 | Unconditional per-start `applyProfileNativeExtensionLaunchArgs` (prefs rewrite + selective CLI) and post-start `maybeMarkExtensionLaunchReady` when assignment already complete; hot-start path that still re-registered packages every open | `v1.7.68` (`40f7978`) | Even after wallets/LES existed, start still walked install/CLI/verify; multi-open paid redundant work and could re-trigger extension activation side effects | Integrity marker `.boost_extension_integrity_v1` + `isExtensionAssignmentComplete` gate: complete → strip CLI only, zero install/verify; one-shot `BrowserExtensionIntegrityScanAll` on client open; assignment change clears marker; create-env / add-extension sync prompts | Integrity unit tests; start-path gate; Windows pack | `git show 40f7978:backend/app_instance.go` always-apply profile native. Do not re-run full extension adapt when integrity marker matches and LES is intact. |

## v1.7.70 read-only extension detect + serial batch start

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-067 | Start-path Preferences rewrite / `installUnpackedExtensionIntoProfile` + `disableUnassignedProfileExtensions` on every open; concurrent batch-start worker pool (5) that finished out of taskbar order | `v1.7.69` (`5cbd104` / `dbb41e8`) | Start mutated user Preferences while only needing presence detection; concurrent starts scrambled Windows taskbar order vs user 编号 | Read-only `extensionAlreadyPresentInProfileReadOnly` / `selectLoadExtensionCLIReadOnly`: cancel CLI when LES or enabled prefs+path exist, never write user data on start; batch start strictly serial by natural name order | Read-only detect tests; CLI skip tests; integrity tests; Windows pack | `git show 5cbd104:backend/extension_profile_install.go` start-time prefs writers; `git show 5cbd104:frontend/.../BrowserListPage.tsx` concurrency=5. Do not rewrite Preferences on start for detect-only. |

## v1.7.71 multi-env sync coordinate / scroll precision

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-068 | `mapScreenPointToFollower` client+outer-origin wheel mapping; render-content always-proportional mapping; popup match using only one expected-left; drop of extension clicks when popup match fails | `v1.7.70` (`bb932a5`) | Wheel/click drift under title-bar offset and non-uniform tiles; extension popup buttons missed or dropped | Shared CM outer/render mapping with same-size absolute; dual expected-position popup search; main-frame fallback when popup missing; scroll delta scaled by W/H when sizes differ; re-assert immediate delay on sync start | Calibration/scroll scale unit tests; Windows pack | `git show bb932a5:backend/app_input_syncer.go` mapScreenPointToFollower / mapChromeInputTarget. Do not drop extension clicks without main fallback. |

## v1.7.72 multi-open tile + wallet-connect sync

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-069 | Sequential per-window `SetWindowPos` tile; trust raw sync-session HWND without main-frame revalidation; 7–9 window 4-column grid; short 50 ms layout-hold under dense multi-open | `v1.7.71` (`319ab6c`) | 10-window + sync occasionally displaced frames: popup HWND tiled, confiner raced half-applied geometry, unstable cell order | `findMainEnvironmentBrowserWindow` + dedupe; `BeginDeferWindowPos` atomic place; natural name sort + master pin; 7–9 as 3×3; longer hold; `ReplaceWindowHandles` in followerIds order | Tile/natural-order unit tests; Windows pack | `git show 319ab6c:backend/app_sync_api.go` sequential SetWindowPos path. Do not tile non-main Chrome widgets. |
| CLEAN-070 | `focusedCDPTarget` fallback that preferred any `chrome-extension://` page when focus probes failed; CDP mouse using physical render pixels as CSS coordinates; popup surface match limited to exact process PID | `v1.7.71` (`319ab6c` / `260cab2`) | Connect Wallet opened only on master: clicks hit wallet docs or missed under 125%/150% DPI; secondary wallet Notification in child process was invisible to input matching | Page-prefer vs extension-prefer target modes; CSS viewport (`innerWidth/innerHeight`) for CDP; process-tree popup ownership for MV3 hosts | Calibration CSS map tests; target helper tests; Windows pack | `git show 260cab2:backend/app_input_syncer.go` focusedCDPTarget extension-first fallback. Never send main-frame dapp clicks to extension documents by default. |

## v1.7.73 tile/sync HWND hard-fail hotfix

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-071 | Title-first `isMainEnvironmentBrowserFrame` that rejected any wallet-brand tab title before size checks; `findProcessTreeWindow` hard-requiring main-frame filter with no legacy largest-window fallback | `v1.7.72` (`1bc7e54` / `93748c9`) | Tile/sync returned zero HWNDs ("没有可用的运行实例窗口" / missing master window) when env main tabs showed MetaMask/Rabby or filters over-rejected | Size-first `environmentFrameLooksLikeMain`; legacy `findProcessWindow` fallback; tile resolve keeps sync-session handle if live resolve fails | Size/title classification unit tests; Windows pack | `git show 1bc7e54:backend/extension_popup_sizer_windows.go` title-first reject; `git show 1bc7e54:backend/win32_helpers.go` filtered-only tree walk. Do not hard-fail tile when a Chrome top-level exists. |

## v1.7.74 uniform 1px-gap tile + CSS wheel proportion

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-072 | Production tile path using large `chromeTileFrameOverlapPx` + outer bleed expand (cells larger than work-area cells and overlapping neighbors); wheel delta scaled only by physical render HWND size | `v1.7.73` (`0206bc8`) | Large overlap looked like uneven gaps; last incomplete-row cells still risked visual size mismatch under DWM; wheel under/over-scrolled when CSS viewport differed from render pixels | `computeUniformTileRects` with `defaultTileGapPx=1` and identical W×H for every cell; wheel scale by master/follower CSS viewport (fallback physical) | Uniform gap/last-size unit tests; scroll scale tests; Windows pack | `git show 0206bc8:backend/app_sync_api.go` frameOverlap/outerBleed tile call; `git show 0206bc8:backend/app_input_syncer.go` physical-only scaleScrollDelta. Do not stretch last tile cell. |

## v1.7.75 visible-tab CDP target + flush tile gap

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-073 | Main-frame `focusedCDPTargetPrefer` fallback that always preferred any `http(s)` page over `chrome-extension://` when OS focus probes failed; wheel path that returned without fallback when follower target match failed; production tile gap of 1px | `v1.7.74` (`c97b529` / `987ca98`) | Master scrolled MetaMask extension home while followers stayed at top (wheel hit wrong CDP document); match miss dropped wheel; user wants near-zero seams | `visibilityState=visible` ranking for active tab (including extension full pages); extension-ID soft match + Win32 wheel fallback; `defaultTileGapPx=0` | Windows pack; scroll on extension full-page manual check | `git show 987ca98:backend/app_input_syncer.go` httpish-first fallback after focus fail. Do not prefer background https over the visible extension tab. |

## v1.7.76 sync-start sole-blank handoff (no post-start tab sweeps)

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-074 | Retired no-op `FinalizeEnvironmentTabsForUserHandoff` that left multi-tab / extension pages open when user started sync; duplicate “post-start continuous tab cleanup” designs from older releases | `v1.7.75` and CLEAN-063 era | User needs one clean blank shell at sync start then full control; start-path CDP sweeps were slow and fought user opens | `prepareEnvironmentsForSyncHandoff` once inside `StartInputSync` (navigate-first sole about:blank); start path remains prefs+session discard only | Plan unit tests; Windows pack | `git show fb385fe:backend/extension_startup_cleanup.go` retired no-op; pre-CLEAN-063 multi-pass cleanup. Do not restore continuous post-start tab watchers. |

## v1.7.77 per-process handoff claim (no re-collapse of user sessions)

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-075 | Collapse-all-participants on every `StartInputSync` regardless of prior handoff | `v1.7.76` (`7e281ce`) | Re-sync or joining new envs wiped tabs the user already opened on previously synced windows | `syncTabHandoffDone` keyed by profileID+pid; atomic `claimSyncTabHandoff`; only brand-new processes collapse; skip already-claimed; clear on stop | Handoff unit tests; Windows pack | `git show 7e281ce:backend/sync_tab_handoff.go` collapse-all-ports. Never re-close tabs on envs already claimed for this process. |

## v1.7.78 dense multi-open sync list + taskbar-safe popups

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-076 | Panel sync list built only from in-memory Running + strict `isProcessAlive`; popup confinement without monitor work-area clamp | `v1.7.77` (`e31a841`) | Assistant stuck ~30 envs while 100+ Chrome windows ran; bottom-row wallet popups sat under the taskbar | Snapshot-first candidate set + HWND re-resolve; accept HWND-valid runtimes; upsert missing profile rows; `clampPopupRectToWorkArea` | Snapshot upsert test; work-area clamp unit test; Windows pack | `git show e31a841:backend/app_sync_api.go` getSyncProfilesLocal. Do not drop live snapshot rows on flaky process-alive alone. |

## v1.7.79 live process scan for sync panel

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-077 | Sync-panel list ownership that required main-client `browser-runtime.json` as the sole/authoritative running set | `v1.7.78` (`968a999`) | Snapshot lag/staleness under dense multi-open left assistant far below real open count | Live `discoverBoostBrowserProcesses` + DevToolsActivePort scan matched by user-data-dir; snapshot optional merge only; broadened process match to app data dir | Windows pack; manual 100+ multi-open refresh | `git show 968a999:backend/app_sync_api.go` snapshot-first getSyncProfilesLocal. Do not treat the JSON snapshot as the only discovery path. |

## v1.7.80 Ads/MoreLogin-style session ownership

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-078 | Auto sole-blank collapse on every `StartInputSync`; unconditional `discardChromeRestorableTabSessions` on every environment start | `v1.7.79` / handoff era | Sync wiped work tabs for envs used outside sync; every start wiped sessions and re-triggered extension surfaces | Input-only sync; session wipe only on first-adapt (`wipeSessions` when CLI still needed); live process scan for panel list | Close-behavior no-op handoff test; Windows pack | `git show 18f6a0c:backend/app_sync_api.go` prepareEnvironmentsForSyncHandoff call. Do not auto-close tabs on sync start. |

## v1.7.81 hot-start zero CLI + live-scan sync list

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-079 | Hot-start still running full `applyProfileNative` CLI decision; session wipe when only prep marker missing; `applyBrowserRuntimeSnapshotData` wipe after live process discovery on StartInputSync/GetSyncProfiles | `v1.7.80` (`9680c4e`) | CLI reinject / session wipe re-opened extension surfaces; snapshot apply cleared 100+ live discoveries down to ~30 | Integrity/hot-settled hard `stripLoadExtensionArgs`; wipe only when `needingInject>0`; live-scan authoritative write-back; no snapshot wipe after scan | Windows pack; multi-open refresh | `git show 9680c4e:backend/app_instance.go` wipeSessions; `git show 9680c4e:backend/app_sync_api.go` apply after live scan. Never wipe live sync candidates with snapshot apply. |

## sync close/reopen repair + dead-code cleanup

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-080 | Retired sole-blank collapse machinery in `backend/sync_tab_handoff.go` (`syncTabHandoffDone`, `claimSyncTabHandoff`, `planCollapseToSoleAboutBlank`, `collapseEnvironmentTabsToSoleAboutBlank`, `prepareEnvironmentsForSyncHandoff`, `syncHandoffTarget`, URL/collapse helpers), `backend/sync_tab_handoff_stub.go`, `backend/sync_tab_handoff_test.go`, and the `clearSyncTabHandoffForProfile` call in `BrowserInstanceStop` | `codex/stable-v1.7.16-rebuild` (working tree, pre-CLEAN-080) | Product policy (v1.7.80+) is input-only sync that never closes or rewrites user work tabs; the collapse path had no production caller (documented “retained for API/diagnostics only”) and kept a per-pid claim registry alive. | `FinalizeEnvironmentTabsForUserHandoff` remains as a no-op Wails binding in `backend/app_sync_handoff_api.go` for old clients; no runtime replacement exists. | Whole-repository symbol scan, full Go tests, Windows cross-build/vet. | Restore a single helper from `git show <pre-cleanup rev>:backend/sync_tab_handoff.go` only together with a real caller and a regression test; never re-add auto-collapse to `StartInputSync`. |
| CLEAN-081 | `computeGaplessTileRects` and `chromeTileFrameOverlapPx` in `backend/tile_layout.go` plus their seven tests in `backend/tile_layout_test.go` | `codex/stable-v1.7.16-rebuild` (working tree, pre-CLEAN-081) | Production tile uses `computeUniformTileRects` with `defaultTileGapPx=0`; the historical overlap/bleed helpers had no caller outside their own tests and invited future tile regressions. | `computeUniformTileRects` is the single production tile owner; uniform-cell tests remain. | Whole-repository caller scan, full Go tests. | Inspect with `git show <pre-cleanup rev>:backend/tile_layout.go`; restore only if a future tile mode needs frame-overlap sizing, with focused tests. |
| CLEAN-082 | `applyBrowserRuntimeSnapshotData` in `backend/browser_runtime_snapshot_windows.go` and the two tests exercising it (`TestPanelSnapshotReplacesProcessLocalRuntimeWithMainClientState`, `TestApplySnapshotCreatesMissingProfileForDenseMultiOpen`) | `codex/stable-v1.7.16-rebuild` (working tree, pre-CLEAN-082) | The live process scan replaced the snapshot-apply path; the function had no production caller (only its own tests), and applying a snapshot after a live scan previously wiped live discoveries (CLEAN-079). | `getSyncProfilesLocal` live scan + panel-only write-back is the runtime owner; snapshot file is an optional merge only. | Whole-repository caller scan, full Go tests, Windows cross-build/vet. | Inspect with `git show <pre-cleanup rev>:backend/browser_runtime_snapshot_windows.go`; do not restore any snapshot-apply wipe after a live scan. |

## project-wide module audit: hidden AutomationPage superseded

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-083 | `frontend/src/modules/browser/pages/AutomationPage.tsx`, its `AutomationPage` export in `frontend/src/modules/browser/pages/index.ts` and `frontend/src/modules/browser/index.ts`, and its route in `frontend/src/App.tsx` | `codex/stable-v1.7.16-rebuild` (working tree, pre-CLEAN-083) | The page was a hidden experimental duplicate: it had no navigation entry and all four of its tabs (接入说明 / 配置管理 / 启动调用 / 日志排障) document the same Launch API with less depth than `LaunchApiDocsPage` (which covers selector, matchMode, error codes, multi-language examples and CDP entry). Keeping both stacked duplicate doc implementations and created two owners for the same protocol. | `LaunchApiDocsPage` at `/browser/launch-api` is the single API-doc owner; the old `/browser/automation` route now redirects there so existing deep links still work. | Frontend production build (TypeScript), no remaining `AutomationPage` references. | Restore with `git show <pre-cleanup rev>:frontend/src/modules/browser/pages/AutomationPage.tsx` only if a separate interactive curl-example experience is explicitly requested; otherwise keep the redirect. |

## v1.7.96 no auto integrity toast + assign must-load

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-084 | `useExtensionIntegrityScanOnOpen` in `frontend/src/App.tsx` (auto `scanExtensionIntegrityAll` + warning toast ~1.2s after client open) and its call from `useWailsNotifications` | `v1.7.95` (`75f67f6`) / pre-`25fde03` | Every client start showed extension "首次适配/未完整" toasts even when the user only needed quiet open; the scan did not fix empty `chrome://extensions` and blocked trust in assign success. | No automatic toast. Backend `BrowserExtensionIntegrityScanAll` / `BrowserExtensionIntegrityDismissNotice` remain for a future optional settings action (default off). Assign requires loadable Preferences; start keeps `--load-extension` until prefs+LES (see `canSkipLoadExtensionCLI`). | Focused Go tests (`TestAssignWrites`, `TestApplyProfileNative`, `TestRegisterAssigned`, `TestReadOnlyDetect`); frontend still builds. | Restore the hook from `git show 75f67f6:frontend/src/App.tsx` only behind an explicit settings toggle defaulting **off**. Do not re-enable auto toast without user opt-in. |
| CLEAN-085 | Preferential CLI-skip based only on Preferences loadable path (`canSkipLoadExtensionCLI` = `isExtensionInstalledInProfile` alone) | `v1.7.95` (`75f67f6`) | Assign wrote Preferences then stripped CLI before Chromium had durable runtime → empty extension list despite "分配成功". | `canSkipLoadExtensionCLI` requires loadable Preferences **and** durable LES/runtime files; register-idempotency uses `isExtensionInstalledInProfile` without stripping CLI early. | Same tests as CLEAN-084. | Inspect `git show 75f67f6:backend/extension_profile_install.go` `canSkipLoadExtensionCLI`; do not restore prefs-only skip without a Cloak load proof. |

## v1.7.97 legacy scan only after delete + dismiss deletes folders

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-086 | Startup auto-scan of orphan Chrome folders in `LegacyDataAutoNotice` (`useEffect` + delayed `scanLegacyDataAuto` on every client open) | `v1.7.96` (`25fde03`) | User does not want import prompts except after deleting an environment; startup noise and confusion | Scan only when `LegacyScanPending` after `BrowserProfileDelete` (event `legacy-data:scan-needed`) or settings force scan | `TestLegacyScanOnlyWhenPending`, `TestLegacyFolderDismissalDeletesAndPersists` | Restore startup scan from `git show 25fde03:frontend/src/App.tsx` only if product reverts to always-on discovery. |
| CLEAN-087 | Dismiss-legacy that only wrote `.boost_notice_dismissed.json` and kept folders on disk | `v1.7.96` | User chose ignore/do-not-import must permanently remove residual data and never re-ask | `BrowserLegacyDataDismissFolders` deletes folders under data root (never registered envs) then records keys | Same tests | Restore keep-on-disk dismiss from `git show 25fde03:backend/app_notice_dismiss.go` only with explicit product approval. |

## v1.7.99 profile-local extension materialize

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-088 | Assign-time "Preferences only, no copy into Default/Extensions" policy (shared absolute package path only) | `v1.7.98` (`513fb98`) | Shared-path prefs + selective CLI still left empty chrome://extensions on user machines; Chrome 137+ also disables --load-extension by default | Materialize package into `Default/Extensions/<id>/<ver>/`, prefs path = local copy; force `DisableLoadExtensionCommandLineSwitch` when CLI load present; keep CLI until LES | `TestInstallUnpackedExtensionIntoProfileMaterializesAndRegisters`, assign/CLI tests | Prefer materialize; shared-path fallback remains if copy fails. |

## v1.7.102 extension loading and manual diagnostics cleanup

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-089 | Startup-triggered data self-check UI and destructive legacy-folder dismissal paths in `frontend/src/App.tsx`, `backend/app_notice_dismiss.go`, and related startup hooks | `v1.7.98` (`513fb98`) | Startup diagnostics interrupted every client open, while the old ignore action could remove recovery folders contrary to the user-data preservation rule. | Read-only manual data self-check beside recovery settings; dismiss records notice state only and never deletes profile, cookie, extension, or wallet folders. | Focused notice/dismiss tests, frontend production build, full Go tests, packaging tests, and Windows cross-check performed for the v1.7.101 change set. | Inspect with `git show 513fb98:frontend/src/App.tsx` and `git show 513fb98:backend/app_notice_dismiss.go`; restore only an explicit user-invoked diagnostic, never startup auto-scan or destructive dismissal. |
| CLEAN-090 | Periodic synchronization-window polling and duplicate profile refresh paths in `frontend/src/modules/browser/pages/WindowSyncPage.tsx` | `v1.7.98` (`513fb98`) | Idle polling repeatedly scanned processes and could overwrite a newer user selection with stale window state. | Action-driven discovery on panel open, refresh, select, master selection, start, and stop boundaries. | Frontend production build plus synchronization regression and full Go test suites for the v1.7.101 change set. | Inspect with `git show 513fb98:frontend/src/modules/browser/pages/WindowSyncPage.tsx`; restore only a cancellable event-triggered refresh with an ordering test, never the timer. |
| CLEAN-091 | Extension-installed inference based on synthetic Chromium Preferences / wallet storage state and the corresponding early CLI-skip branches in `backend/app_extension_import.go` and `backend/app_instance.go` | `v1.7.98` (`513fb98`) | Synthetic state could report success while Chrome loaded no extension, producing an empty extension toolbar after restart. | The saved assignment is authoritative; assign-time materialization writes a profile-local package, and the launch path keeps `--load-extension` until durable runtime evidence exists without overwriting existing LES, Cookies, IndexedDB, or wallet storage. | Materialization, duplicate/no-overwrite, launch-argument, full Go, frontend, packaging, and Windows compilation checks for the v1.7.101 change set. | Inspect the two files with `git show 513fb98:<path>`; restore only a read-only detector after proving it cannot suppress required loading or mutate user data. |

## v1.7.105 generic extension assignment activation

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-092 | Assignment path that treated a saved `--load-extension` argument as completed installation while deliberately leaving stopped profiles unregistered; production start path that skipped the generic native-extension activation helpers | `v1.7.104` (`e5f34e2`) | The manager reported assignment success although Chrome could open with an empty extension list; different extensions appeared inconsistent depending on whether Chromium happened to retain usable runtime state. | Explicit assignment materializes and registers every generic unpacked extension in stopped profiles; running profiles defer safely to restart; launch always applies the common native-extension decision and Chrome 137+ compatibility switch. No wallet brand allowlist or data-store rewrite is used. | Focused assignment/healing/launch tests, full Go suite, Windows amd64 cross-compile, frontend production build, packaging tests, and `git diff --check`. | Inspect `git show e5f34e2:backend/app_extension_import.go` and `git show e5f34e2:backend/app_instance.go`. Restore only if Chromium gains an independently verified registration API; never restore LaunchArgs-only success reporting. |

## v1.7.107 updater download route fallback ledger

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-093 | Single-client `DownloadUpdate` path that stopped after the first selected proxy failed, plus implicit fallback discovery inside an explicitly selected direct client | `v1.7.105` (`c170f5d`) | A proxy could reach the small Release/version response but fail on GitHub's large asset CDN, leaving clients able to detect a version but unable to download it. | Bounded ordered routes: client proxy, deduplicated environment proxy, Windows system proxy, then intentional direct; each attempt owns and closes its response/temp file, and only a SHA256-matching PE is accepted. | Route-priority tests, full Go suite, Windows amd64 cross-compile, frontend production build, packaging tests, and `git diff --check`. | Inspect `git show c170f5d:backend/app_updater.go` and `git show c170f5d:backend/safe_remote_http.go`; restore only for diagnosis, never as the production updater because it loses fallback after the first route. |

## Unreleased extension assignment safety and idle-polling cleanup

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-094 | The unused `startBrowserRuntimeReconciler` 2-second perpetual process scan and its dormant startup reference | `v1.7.108` (`761b55d`) | It had no production caller and contradicted the action-driven synchronization rule; retaining it made a future accidental re-enable likely. | `RefreshBrowserRuntimeState` and synchronization-panel refresh remain explicit user actions. Live process discovery is run only when the panel opens, refreshes, selects a master, or starts synchronization. | Whole-repository symbol scan, full Go suite, static vet. | Restore the function from `git show 761b55d:backend/browser_runtime_recovery_windows.go` only with an explicit user-facing recovery mode and a cancellation/idle-cost test; never start it automatically. |
| CLEAN-095 | Unconditional known-extension rebinding and destructive replacement of a non-empty invalid profile extension version directory | `v1.7.108` (`761b55d`) | A repeated assignment could touch an already loadable profile extension, while deleting an unexpected directory could destroy Chrome-owned extension files and risk wallet state recovery. | Saved assignments first skip any profile that already has an equivalent loadable extension. A valid profile-local package is reused; an invalid non-empty directory is preserved and the verified shared package is registered as a fallback. Package copies remain assignment-time only. | New duplicate/no-overwrite and invalid-directory preservation tests; full Go suite; frontend production build. | Inspect `git show 761b55d:backend/extension_integrity.go` and `git show 761b55d:backend/extension_profile_install.go`; never restore deletion or overwrite of an existing profile extension directory. |

## v1.7.109 window recovery and proxy route simplification

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-096 | Auto-mode local VPN gateway-first probe for every external environment proxy | `v1.7.108` (`761b55d`) | A directly reachable proxy or TUN-routed proxy was unnecessarily wrapped in an additional local gateway hop, increasing latency and creating avoidable routing conflicts. | Auto mode probes the environment proxy first and uses a local VPN gateway only as a bounded fallback. Explicit `local_gateway` remains opt-in. | Proxy relay suite, full Go suite, vet, frontend production build. | Restore only if measurements prove direct/TUN routing is unavailable for all supported employee networks; do not restore gateway-first routing as the automatic default. |

## v1.7.111 persistent extension package migration

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-097 | Install-root `extensions/imported` ownership and its folder-exists-only global distribution completion path in `backend/app_extension_import.go` | `v1.7.109` (`2e39e79`) | The installer/repair process may replace the program directory while preserving profiles, leaving valid wallet and Chrome data pointing at missing extension program files. A historical completion record could also hide a profile whose package was no longer loadable. | `data/extensions/imported` is the persistent package store. A bounded first-start migration copies only manifest-validated package code from the old store or profile `Default/Extensions`; explicit distribution verifies each target profile before skipping. Old packages and all profile data remain untouched. | Focused package migration and external-path tests, full Go suite, frontend production build, Windows cross-build/vet, and `git diff --check`. | Inspect `git show 2e39e79:backend/app_extension_import.go`; restore only `globalExtensionDir`/`installUnpackedExtension` path handling if a legacy installation layout requires it. Never restore program-directory ownership or folder-exists-only success inference. |

## v1.7.112 Chromium-owned extension state

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-098 | Active startup calls that rewrote Chromium profile state: `seedDefaultSearchEngine`, `ensureCloakLocalStateFlags`, post-start CDP seed retry, and the client-managed `--load-extension` injection path in `backend/app_instance.go` | `v1.7.111` (`f20a8bf`) | Direct writes to browser-owned `Preferences`, `Local State`, fixed tabs or extension settings can trigger Chrome's external-change reset, hiding user-installed extensions and wallets. | Environment startup is read-only for Chromium-owned profile files. It preserves user-supplied launch arguments and performs only a bounded in-memory recovery argument for Chrome-created profile extension folders whose registration is missing. | Focused recovery/assignment tests; full Go suite; frontend production build; packaging tests; source scan for removed active calls. | `git show f20a8bf:backend/app_instance.go`; restore only a Chromium-supported public registration API with a regression test proving it does not modify browser-owned settings. |
| CLEAN-099 | Direct assignment-time Chromium Preference registration/developer-mode writes and profile disable writes in `backend/app_extension_import.go` | `v1.7.111` (`f20a8bf`) | The manager must not alter a user's existing Web Store extension registration or wallet identity to report a successful assignment. | Explicit manager actions record their own metadata only; user-installed profile extensions are detected read-only and always take priority. Repeated explicit global actions skip profiles already recorded for the same manager package. | Focused explicit-assignment tests verify no `Preferences` create/rewrite; full Go suite. | `git show f20a8bf:backend/app_extension_import.go`; restore only the smallest needed operation after Chrome exposes a safe registration mechanism, and add a no-profile-write test. |
| CLEAN-100 | `backend/extension_startup_cleanup.go`, `backend/extension_startup_page_cleanup.go`, `backend/environment_popup_confinement_windows.go`, `backend/environment_popup_confinement_other.go`, and their continuous/post-start extension-popup cleanup ownership | `v1.7.111` (`f20a8bf`) | Closing or constraining extension pages after startup conflicts with normal wallet/web-page interaction, creates extra CDP work for every environment, and can race user actions. | No startup popup cleaner or popup watcher remains. Synchronization continues to route user input; only explicit layout hold state remains during a user-requested layout operation. | Packaging static checks reject CDP target-close/discovery startup cleanup; focused sync and full Go tests. | `git show f20a8bf:backend/extension_startup_page_cleanup.go` or `git show f20a8bf:backend/environment_popup_confinement_windows.go`; restore only an explicit user-invoked action with no background worker and a bounded test. |

## v1.7.114 legacy extension launch restoration

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-101 | Blanket `stripBrowserStudioManagedExtensionLaunchArgs` runtime suppression and the automatic Cloak Web Store helper injection in `backend/app_instance.go` | `v1.7.113` (`53d8a0a`) | The suppression discarded the only saved package path for older assigned extensions, leaving Chrome extension data in the environment but no executable extension package at launch. The automatic marketplace helper then appeared instead of the user's original extension. | Preserve the environment's existing sanitized `--load-extension` arguments for this launch, enable Chromium's documented CLI compatibility feature when needed, and retain the bounded read-only recovery for profile-owned packages. No browser-owned file is written and no helper is auto-injected. | `TestProfileExtensionRecoveryKeepsSavedManagedPackageAndEnablesCLI`, recovery tests, full Go suite, frontend build, Windows cross-build and packaging tests. | Restore only the smallest relevant helper from `git show 53d8a0a:backend/profile_extension_recovery.go` if a future Chromium core proves that saved package paths are unsafe; never restore blanket suppression without a user-data preservation regression test. |

## v1.7.115 active Chromium profile extension recovery

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-102 | Default-only recovery scan and cross-profile aggregate `Preferences` extension-state lookup in `backend/profile_extension_recovery.go` | `v1.7.114` (`88cfcb4`) | A registration in `Profile 1` could falsely suppress recovery for an extension package in the environment's actual startup profile, while profiles other than `Default` were never searched for a legacy manager package. This produced an empty `chrome://extensions` despite intact environment data. | Determine the profile for this launch from explicit `--profile-directory` or read-only `Local State`, inspect only that profile's `Preferences` and `Extensions` folder, and preserve legacy program packages from standard `Profile N` folders as well as `Default`. | Selected-profile, Local State, blocker-removal, mixed-case CLI, and Profile 1 package migration tests; full Go suite; frontend production build; Windows cross-build/vet and packaging tests. | Inspect `git show 88cfcb4:backend/profile_extension_recovery.go` and `git show 88cfcb4:backend/extension_package_store.go`; restore only the selected-profile fallback if Chromium's documented profile choice changes. Never restore cross-profile state merging or write Chromium-owned JSON. |

## v1.7.117 signed same-ID extension recovery

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-103 | Keyless `Default/Extensions/<id>/<version>` acceptance as sufficient identity in `recoverableProfileExtensionDirsForLaunch` | `v1.7.116` (`650791d`) | A Chrome Web Store manifest usually omits its public key. Loading that folder unpacked derives an ID from its filesystem path, creating a different extension that cannot reconnect the original wallet/account storage. It also fails completely when an upgrade/reset has emptied `Default/Extensions`, as observed in the affected v1.7.115 profile. | Persist an ID-only per-profile inventory under `data/`; require durable same-ID Local Extension Settings/IndexedDB plus missing Chromium registration; download the signed Web Store CRX, verify its public-key-derived ID, install it in the persistent package store, and use verified Chrome for Testing 148 for recovery. | Keyless-package rejection, exact-ID inventory/orphan/Secure Preferences tests, Chrome 148 preference test, focused Go suite, full verification matrix for v1.7.117. | Inspect `git show 650791d:backend/profile_extension_recovery.go`. Restore only the directory scan, never the keyless identity assumption; any offline recovery package must carry a manifest key whose derived ID exactly matches the recorded extension ID. |

## v1.7.118 UUID data identity and absolute synchronized zoom

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-104 | Caller-supplied `ProfileInput.UserDataDir` ownership during ordinary `Manager.Create`; relative Ctrl+wheel replay to follower pages | `v1.7.117` (`0b63d67`) | Ordinary creation could diverge the generated environment UUID from its physical data folder. Relative zoom replay also preserved any pre-existing difference between the master and follower zoom levels. | New environments always use `data/<ProfileId UUID>`. Startup/update attach an existing matching UUID directory in place and reject ambiguous dual-live paths. Ctrl+wheel measures the master's final render/CSS scale and converges followers to that absolute value. | UUID creation/reconciliation/conflict/state-preservation tests; absolute zoom direction test; full Go suite; Windows cross-build; frontend and packaging verification. | Restore caller-directed data placement only through the existing explicit import/recovery APIs, never ordinary creation. Restore relative Ctrl+wheel only if Chrome removes measurable viewport scaling, while retaining a regression that starts followers at different zoom levels. |

## v1.7.119 update-only integrity lifecycle

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-105 | Ordinary-client startup UUID/data attachment, extension identity capture, legacy package migration, kernel-directory scans and archive retention scan; per-environment-start same-ID extension scan/download and temporary recovery-core switch | `v1.7.118` (`6e46a35`) | Running integrity and recovery work on every client/environment open delayed users and allowed one maintenance rule to repeatedly touch normal usage. Extension recovery could perform network work in the environment launch critical path. | The updater's explicit `--post-update` process owns one maintenance pass: attach matching UUID data in place, preserve/read extension IDs, prepare verified same-ID packages, persist recovered launch paths, set every environment and the global default to the fixed bundled Chrome for Testing 148 path, and update only BrowserStudio identity pointers. Normal client/environment startup performs none of these scans. A new private installation receives the fixed 148 default directly from packaged configuration. | `TestPostUpdateMaintenanceAlignsChrome148AndPreservesBrowserOwnedState`; packaging lifecycle gate; full Go/frontend/Windows/packaging matrix. | Inspect `git show 6e46a35:backend/app.go` and `git show 6e46a35:backend/app_instance.go`. Restore only an explicit update/manual action; never restore automatic integrity, extension recovery, kernel scanning or downloads to ordinary startup/environment launch. |

## v1.7.120 user-owned cache maintenance

| ID | Removed path | Last known revision | Reason | Replacement | Verification | Precise recovery |
| --- | --- | --- | --- | --- | --- | --- |
| CLEAN-106 | Default-on fixed 7-day cache policy and the synchronization-panel call to `startCacheAutoCleanScheduler` | `v1.7.119` (`15dd68f`) | Main client and sync panel could own the same filesystem task. The panel does not own authoritative Running/PID state, so its first due pass could scan caches belonging to live Chrome environments and saturate a mechanical data disk. | New installs and upgrades default to manual mode. Settings offers manual or a user-selected 1-365 day interval. Only the main client schedules cleanup; every run also checks real Chrome `--user-data-dir` processes and fails closed when Windows process discovery cannot verify safety. | Focused config, owner, interval, live-user-data guard and data-preservation tests; full Go suite; frontend production build; Windows amd64 test compilation/build/vet; packaging tests; `git diff --check`. | Restore the v1 scheduler from `git show 15dd68f:backend/app_cache_cleanup.go` only for historical comparison. Never restore panel ownership, fixed forced interval, or cleanup based solely on in-memory Running/PID state. |
