# BrowserStudio v1.7.40

## 环境代理与本地 VPN 链路

- 优先完善带账号密码的 HTTP、HTTPS、SOCKS5 环境代理，不要求用户使用 Clash YAML 订阅。
- 新增自动、本地 VPN 网关、TUN、直连四种网络链路模式；不改变现有代理池 ID、环境绑定或已导入代理。
- 非 TUN 模式可显式使用本机 VPN 的 HTTP/SOCKS5 监听端口作为第一跳，随后连接环境指定的 IP 代理；网站出口仍为环境代理 IP。
- 自动识别常见 Clash Verge Rev、Clash、v2rayN 本机端口，也允许用户填写 `http://127.0.0.1:端口` 或 `socks5://127.0.0.1:端口`。
- TUN 模式继续支持由系统虚拟网卡接管第一跳；直连模式可明确绕过本地 VPN 网关。

## 稳定性与性能

- 本地 VPN 网关探测并发执行并缓存 5 分钟，20 个环境同时启动只执行一次探测，避免重复等待。
- 环境代理协议、账号密码验证与浏览器实际流量共用相同首跳链路，避免测速成功但浏览器无网络。
- HTTP 首跳使用 CONNECT 隧道并支持 Basic 认证；SOCKS5 首跳支持账号密码和远端域名连接。
- 仅允许本机回环地址作为 VPN 首跳，避免误把远程代理再次当作本地网关形成错误链路。

## 升级

- v1.7.18–v1.7.39 可直接在线升级到 v1.7.40，无需逐版本安装。
- 升级继续保留现有 `data`、环境编号、Cookies、扩展、钱包扩展数据、代理池、内核与激活状态。
