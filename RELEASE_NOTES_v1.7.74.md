# BrowserStudio v1.7.74

## 平铺布局

- 窗口之间固定 **1px 间隙**（不再用大面积重叠）。
- **所有格子尺寸相同**，最后一行未填满的窗口也不再被拉宽。

## 同步滚动

- 上下/横向滚轮按 **CSS 视口比例** 缩放 delta（优先 `innerWidth/innerHeight`）。
- 同尺寸平铺 1:1；尺寸不同时按可见页面比例滚动，减少跟随不同步。

## 含近期修复

- v1.7.73：平铺/同步 HWND 过严导致找不到窗口的热修。

## 升级说明

- 支持从 v1.7.73 及更早版本在线升级到 v1.7.74。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展包库、代理池、浏览器内核或激活数据。
