# BrowserStudio v1.7.61

## 标签页管理

- 启动时：短时定点（非长监听）关闭 MetaMask 等扩展自动打开的引导页；不触碰浏览器默认空白页。
- **新增用户触发一锤子**：打开同步工具、同步面板首次打开、或环境列表在有运行环境时的**第一次点击**，对所有运行中环境执行：
  - 关闭扩展页与其它内容页
  - 只保留 **一个 `about:blank`**
- 不做会话级实时扫描，避免误关用户稍后主动打开的扩展页。

## 说明

- MetaMask onboarding 由扩展自身打开，不是客户端每次重扫扩展列表导致。
- 稳定扩展 ID / 钱包数据对齐后，引导页会减少；本版保证启动与同步前可自动清掉多余页。

## 升级说明

- 支持从 v1.7.60 及更早版本在线升级到 v1.7.61。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展、代理池、浏览器内核或激活数据。
