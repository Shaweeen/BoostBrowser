# BrowserStudio v1.7.46

## 扩展弹窗自适应修复

- 删除钱包弹窗固定 `390×620` 尺寸，扩展程序继续使用自身的自然窗口大小。
- 平铺、堆叠和横排后，二级、三级窗口仅在越界时收纳到所属环境区域，不再继承固定比例。
- 暂停键鼠同步时继续维持弹窗区域约束；停止同步时仍会释放同步助手的窗口监听资源。
- 修复暂停同步后 Rabby 等钱包弹窗重新出现大面积无效白色画布的问题。

## 启动窗口边界

- 删除 Chrome 进程级 `--window-size=1400,600` 参数，防止后续创建的扩展窗口继承主环境启动尺寸。
- `1400×600` 仅由 Windows 启动阶段短时应用于主环境窗口。
- 用户执行平铺、堆叠或横排后，会立即取消尚未结束的启动尺寸定位。
- 环境启动时不再自动展示全部扩展的 onboarding、解锁或欢迎页面，默认只保留一个 `about:blank` 初始页。
- 扩展仍保持安装和启用状态；用户启动完成后主动点击扩展图标不受影响。

## 代码健康与升级兼容

- 删除旧的标题专用弹窗尺寸分支，没有增加新的后台扫描器或窗口监听器。
- 添加自然尺寸、排列边界、暂停状态和启动参数隔离的回归测试。
- 可从 v1.7.45 或更早版本直接在线升级到 v1.7.46。
- 升级继续保留 `data`、环境编号、Cookies、浏览器账号、扩展及钱包数据、代理池、内核和激活状态。
