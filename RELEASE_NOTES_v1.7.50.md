# BrowserStudio v1.7.50

## 环境启动标签修复

- BrowserStudio 不再通过命令行、启动网址或首选项创建任何默认空白标签，唯一初始空白页由浏览器内核自然创建。
- 每次启动或重新启动环境时，在浏览器调试入口就绪后的限定启动阶段内，立即关闭扩展自动打开的顶层欢迎页、解锁页和多余空白页，只保留一个自然空白页。
- 检查以浏览器实际启动标签为准，不再依赖启动参数中的扩展 ID，因此兼容旧环境中已经持久化安装的 MetaMask、Rabby、Jupiter 等扩展。
- 扩展后台页、Service Worker、扩展数据、钱包数据、Cookie 和登录状态均不关闭、不覆盖；用户稍后点击扩展图标时仍可正常使用。
- 启动收敛完成后立即断开 CDP 连接并释放全部临时状态，不创建常驻监听、后台轮询、计时器或窗口注册表。

## 升级说明

- 支持由 v1.7.49 及更早版本直接在线升级到 v1.7.50。
- 升级不会覆盖现有 `data`、浏览器环境、扩展、钱包、Cookie、代理池或激活数据。
