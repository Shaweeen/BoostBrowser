# BrowserStudio v1.7.120

## 缓存管理与资源占用修复

- 删除默认开启、固定 7 天的自动缓存清理策略；新安装和升级后默认使用“仅手动清理”。
- 设置页新增“仅手动清理 / 按周期自动清理”，自动周期可由用户设置为 1–365 天，并提供“立即清理”。
- 缓存调度器只允许主客户端拥有；独立同步工具不再启动或执行相同的缓存任务。
- 每次自动或手动清理前，除内存中的 Running/PID 状态外，再按真实 Chrome 进程的 `--user-data-dir` 校验；无法确认进程状态时取消清理。
- 运行中的环境始终跳过。Cookies、登录状态、IndexedDB、Local Storage、扩展与钱包数据继续受保护。

## 验证

- 缓存数据保护、用户自定义周期、同步工具 owner 隔离、真实 UserDataDir 二次保护回归测试通过。
- 完整 Go 测试、前端生产构建、Windows amd64 测试编译/主程序构建/vet 和打包脚本测试通过。
