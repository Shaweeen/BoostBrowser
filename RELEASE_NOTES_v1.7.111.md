# BrowserStudio v1.7.111

## 修复内容

- 补齐扩展包持久化迁移的删除台账与发布预检：升级后，已分配扩展的程序包保存在 `data/extensions/imported`，不再依赖可被安装器替换的主程序目录。
- 自动恢复只复制通过 `manifest.json` 验证的扩展程序代码；不会删除旧扩展包，也不会读取、覆盖或迁移 Cookies、钱包、Local Extension Settings、IndexedDB、助记词或私钥。
- 用户主动点击“分配”时会按实际环境的可加载状态补齐缺失扩展；已有可用扩展不会被替换。
- Cloak/隔离内核的 Chrome 网上应用店桥接仍仅按需加载；普通 Chrome 不会被写入该桥接。

## 验收方式

1. 从旧版更新后，打开已有环境并在 `chrome://extensions` 确认已分配扩展仍可见、启用且钱包状态未改变。
2. 检查 `data/extensions/imported`：已恢复的扩展包会位于此目录；旧安装目录下的扩展包不会被删除。
3. 对新建环境执行一次“分配”，仅缺失扩展会被添加，原有扩展和钱包数据不应被覆盖。
