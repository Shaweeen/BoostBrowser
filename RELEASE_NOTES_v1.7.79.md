# BrowserStudio v1.7.79

## 同步工具：实时扫描已启动环境

- **不再依赖** 单一 `browser-runtime.json` 作为唯一数据源。
- 刷新时 **扫描本机已启动的 Chromium 进程**（`--user-data-dir` + `--remote-debugging-port`），与配置环境目录匹配。
- 辅以各环境目录下的 `DevToolsActivePort` 探测；快照文件仅作可选合并提示。
- 对发现的全部 PID **批量解析主窗口**，减少「运行中很少、任务栏很多」的偏差。

## 含近期修复

- v1.7.78：底行钱包弹窗避开任务栏遮挡。

## 升级说明

- 支持从 v1.7.78 及更早版本在线升级到 v1.7.79。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展包库、代理池、浏览器内核或激活数据。
