# BrowserStudio v1.7.29

## Esc 全局暂停与恢复同步

- 同步功能运行期间，第一次按下 `Esc` 会立即静默暂停鼠标、键盘、页面、滚动、URL 与弹出菜单约束同步，不显示暂停提醒。
- 再次按下 `Esc` 会恢复原同步会话，并显示 1 秒绿色“同步功能已恢复”提示；无需重新选择主控或跟随环境。
- 按键只在同步会话运行期间生效，并拦截系统长按产生的重复按键，避免一次按键被切换多次。
- 暂停和恢复会使切换前已排队或延迟的同步动作失效，防止旧点击、旧按键在恢复后补发。

## 多窗口性能与稳定性

- 20 个及以上跟随窗口启用自适应鼠标移动降采样，降低大量窗口同时接收高频消息造成的卡顿。
- 弹出菜单边界检查按窗口数量自适应调度，减少大批量窗口下反复扫描系统窗口的开销。
- 启动同步时复用一次运行状态快照和一次批量窗口枚举，避免昂贵的进程恢复扫描与窗口加载并发竞争。
- 修复同步页面首次加载时可能重复读取全部环境的问题，并保持环境编号的既有稳定顺序。

## 兼容性

- 保持 v1.7.28 的 data、Cookies、扩展、钱包数据、浏览器内核和激活状态保护规则。
- 在线更新只替换程序文件，不覆盖用户数据。
