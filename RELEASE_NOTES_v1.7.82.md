# BrowserStudio v1.7.82

## P0：同步会话自愈——关闭后重开、多开环境准确识别与同步

- **关闭→重开自愈**：同步进行中关闭某个环境再重新打开时，同步会话自动重新解析该环境的新窗口/新 PID/新调试端口，输入同步与 URL 同步**无需重启会话**即可继续。
- **平铺前实时刷新**：点排列前先刷新实时运行状态，刚新开/重开的环境立即参与平铺，不再出现「明明已打开却排不进去」。
- **端口对齐**：跟随窗口的 HWND 与 CDP 调试端口严格 1:1 对齐；跟随环境被关闭剔除时同步剔除对应端口，按键/URL/滚动同步**永不错位到错误环境**。
- **运行状态所有权收敛**：同步面板(独立进程)才做实时扫描写回；主客户端不再被瞬时扫描漏检把运行中的环境误判为已停止。

## P1：同步列表与主窗口判定

- 显式刷新(`RefreshSyncSnapshot`)单次扫描，关闭一个环境立刻打开另一个时不再返回旧列表。
- 环境启动后把已验证的主窗口登记到窗口同步注册表，平铺优先采用注册的主窗口，**绝不把钱包/OAuth 弹窗当环境排进网格**；环境停止时同步移除登记。
- 指纹种子稳定性：编辑/启动不再悄悄更换浏览器身份种子（防社交平台登录失效）；缺失种子时由环境 ID 派生稳定种子。

## 升级说明

- 支持从 v1.7.81 及更早版本在线升级到 v1.7.82。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展包库、代理池、浏览器内核或激活数据。
