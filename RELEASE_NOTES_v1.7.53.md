# BrowserStudio v1.7.53

## 环境数据与钱包存储保护

- 环境编号、Profile ID 与用户数据目录保持稳定的一一对应；普通编辑、启动默认值或代理配置写入失败时不会改变现有数据归属。
- 环境关闭先请求 Chromium 正常退出，并等待进程、调试端口和用户数据锁释放后再记录关闭完成，避免 Cookie、扩展数据库或钱包状态尚未落盘便终止进程。
- 删除环境改为可恢复归档；完整浏览器目录和非敏感身份映射保留在恢复区，不会把钱包扩展存储作为缓存直接删除。
- 旧数据、备份和快照恢复均先在独立目录完成验证，再原子切换；失败时保留当前数据和恢复索引。

## 扩展程序稳定性

- 扩展仍只在用户明确点击“分配”时处理，不增加启动扫描、后台轮询或自动覆盖。
- 已运行环境不会被并发改写 Chromium `Preferences`；现有 Cookie、扩展配置和加密钱包存储保持原样。
- 扩展程序包更新保留上一份可用程序包；安装或持久化失败会恢复文件与环境配置，不留下数据库和界面状态不一致。
- 浏览器启动仍只执行一次启动阶段标签清理；用户、网页或应用稍后主动打开的钱包扩展页面不受影响。

## 多环境与弹窗

- 扩展、授权及钱包弹窗归属于各自环境窗口，在同步排列中保持自然内容尺寸并限制在对应环境区域，不再作为桌面全局置顶窗口覆盖其他环境。
- 同步、关闭和弹窗约束维持单一生命周期所有者，不新增重复监听器或常驻扫描任务。

## 升级说明

- 支持从 v1.7.52 及更早版本直接在线升级到 v1.7.53。
- 升级不会覆盖现有环境、Cookie、登录状态、钱包、扩展、代理池、浏览器内核或激活数据。
- 如文件系统或数据库提交失败，操作将明确失败并保留可恢复数据，不会继续执行破坏性覆盖。
