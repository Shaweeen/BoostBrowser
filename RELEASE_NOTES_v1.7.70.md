# BrowserStudio v1.7.70

## 扩展启动（只读检测，取消重复 CLI load）

- 启动时**只读**检测环境是否已有扩展（LES / 启用的 Preferences + 有效 path）。
- **已有扩展 → 取消该包的 `--load-extension`**，减少重复弹标签页。
- **尚无 data → 保留 CLI 做首次适配**。
- 启动路径**不写** Preferences、不改 LES/Cookies、不拷贝扩展包（尊重用户数据）。

## 批量启动

- 多选启动改为按环境**编号自然序严格串行**（不再 5 路并发）。
- 任务栏图标按编号顺序出现，避免排列紊乱。

## 含 1.7.69 能力

- 扩展完整性标记与客户端打开一次巡检。
- 新建环境 / 分配扩展时的同步询问。

## 升级说明

- 支持从 v1.7.69 及更早版本在线升级到 v1.7.70。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展包库、代理池、浏览器内核或激活数据。
