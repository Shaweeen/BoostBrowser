# BrowserStudio v1.7.54

## 扩展启动标签修复

- 修复批量启动环境时 MetaMask 等钱包扩展延迟创建标签、未被启动阶段清理的问题。
- 正常调试就绪与延迟调试就绪两条互斥路径现在共用同一个启动标签收敛流程，不再漏掉部分窗口。
- 每个环境只在窗口仍最小化、尚未交给用户的启动事务内短暂检查；完成后立即释放，不增加常驻监听、后台扫描或长期资源占用。
- 保留浏览器内核自然创建的唯一空白页，并移除扩展自动打开的页面及多余空白页。

## 扩展与用户数据保护

- 仅关闭环境本次启动期间由扩展自动创建的顶层页面，不读取网页内容、表单、助记词、密钥或钱包数据。
- 扩展后台 Service Worker、扩展配置、Cookie、登录状态和加密钱包存储保持不变。
- 环境窗口交给用户后，用户点击扩展、网页请求钱包连接或应用主动拉起的扩展弹窗均可正常使用，不受启动清理影响。

## 升级说明

- 支持从 v1.7.53 及更早版本直接在线升级到 v1.7.54。
- 升级不会覆盖现有环境、编号、Cookie、登录状态、钱包、扩展、代理池、浏览器内核或激活数据。
