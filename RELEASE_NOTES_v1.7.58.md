# BrowserStudio v1.7.58

## 启动速度

- 环境启动页清理恢复为 v1.7.48 式**单次快速扫描**：调试就绪后立即关闭扩展自动页/多余空白页并确保一页空白，**不再阻塞数秒**。
- 延迟出现的钱包扩展启动页改为**后台短重试**关闭，不拖慢多开返回。

## 在线升级说明

- 客户端「检查更新」只读取 GitHub 公开 Release（`boost-browser.exe` + sha256），与 Private 带内核安装包**不是同一通道**。
- 若仅打 Private 包未执行 `publish_windows_github_release.ps1`，用户端会一直显示当前 Latest（例如 1.7.55）为最新，无法在线升到本版本。
- 在线升级只替换主程序/更新器相关文件，不重装内核；完整带内核环境请使用 Private Setup 安装。

## 升级说明

- 支持从 v1.7.57 及更早版本在线升级到 v1.7.58（需已发布对应 GitHub Release）。
- 升级不覆盖现有环境、Cookie、登录状态、钱包、扩展、代理池、浏览器内核或激活数据。
